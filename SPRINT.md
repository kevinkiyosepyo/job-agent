# Job Agent Build Backlog

Started: 2026-08-23 11:34 PDT
Ends: approximately 2026-08-23 15:34 PDT

## Verified baseline

- 14 tests passing.
- Live Google Workspace OAuth check succeeds.
- Sheets append/read-back/cleanup integration succeeds; production tracker restored to 26 rows.
- Discord notification send/read-back succeeds.
- Greenhouse and Workday fixture workflows verified without submission.
- Daily production job is enabled for 08:00 PDT.

## Post-backlog build queue — completed

All fifteen ordered backlog tasks below have verified green evidence in the sprint log. Do not add speculative micro-slices; a new prioritized queue is required before further implementation.

1. Build an executable Greenhouse handler behind a shared ATS interface, matching the verified Lever/Oracle safety contract.
2. Build an executable Workday handler behind the same interface with wizard-step, resume-parse, and save-draft semantics.
3. Add an ATS handler registry and a single `prepare-job` command that dispatches Greenhouse, Workday, Lever, and Oracle while remaining non-submitting by default.
4. Integrate prepared ATS plans with the leased queue worker and a durable execution journal so crash recovery resumes the correct step.
5. Add a versioned source registry for approved Greenhouse/Lever boards and deterministic registry-driven collection.
6. Add Kevin Bible Google Doc synchronization into a local ignored cache with source timestamps, resume-precedence conflict reporting, and no raw PII in logs.
7. Add an answer-coverage preflight matrix that reports known, company-specific, optional-skip, and human-required questions before opening a form.
8. Wire Discord job-ID approval commands to real queue transitions with authorization, expiry, replay protection, and audit events.
9. Add bidirectional queue/Google-Sheet state reconciliation that detects drift without overwriting newer verified states.
10. Add stale/closed posting detection and terminal queue closure before form preparation.
11. Add per-ATS rate limits, retry budgets, and circuit breakers so a failing tenant cannot stall the entire worker.
12. Add a machine-readable operational health report covering sources, browser, OAuth, queue leases, tracker drift, and notification delivery.
13. Add resume selection and filename/content preflight so the chosen resume is confirmed before each preparation.
14. Run non-submitting end-to-end fixtures for every handler through source → queue → answer engine → ATS plan → evidence artifact.
15. Run a final production-readiness audit and idempotent dry run; document any remaining human-only gates instead of weakening safeguards.

Profile confirmations applied 2026-08-24: permanent Fairfax address/current San Diego, application license state CA, outside-business answer No, exact Qualcomm/HDSI/MyEMSPath/Handshake dates with day 1 policy, and Kevin Bible source precedence. HNRC exact start month remains unknown and must fail closed if a form requires it.

## Sprint log

Autonomous runs append concise verified entries here. Never report work as complete until tests or real read-back verification pass.

- 2026-08-23 11:58 PDT — Completed task 1: added `app_queue.py` SQLite queue with normalized-URL idempotent enqueue plus explicit `discovered -> prepared -> applied` transition rules. Verified with strict RED→GREEN tests in `tests/test_app_queue.py` and full suite: `python -m pytest tests -q` → `17 passed in 0.03s`. Next task: structured JSONL audit logging with redaction.
- 2026-08-23 18:31 PDT — Completed task 2: added `audit_log.py` structured JSONL audit logger with recursive redaction for sensitive fields like email, phone, token, and resume paths. Verified via strict RED→GREEN in `tests/test_audit_log.py` (`pytest tests/test_audit_log.py::test_log_event_writes_jsonl_and_redacts_sensitive_values -v`) and full suite: `pytest tests -q` → `18 passed in 0.05s`. Next task: top-level dry-run orchestration CLI.
- 2026-08-23 18:44 PDT — Completed task 3: added `orchestrator.py` dry-run CLI that reuses scanner classification, routes via `pipeline.py`, stages supported Greenhouse/Workday jobs into the local SQLite queue as `discovered`, and writes a redacted audit event plus JSON report. Verified via strict RED→GREEN in `tests/test_orchestrator.py` (`python -m pytest tests/test_orchestrator.py::test_dry_run_orchestrates_scan_route_queue_and_audit -v`) and full suite: `python -m pytest tests -q` → `19 passed in 0.04s`. Next task: public Greenhouse and Lever source adapters with bounded retries.
- 2026-08-23 18:58 PDT — Completed task 4: added `sources.py` with public Greenhouse and Lever adapters that normalize internship postings and retry transient `TimeoutError`s up to a bounded attempt budget. Verified via strict RED→GREEN in `tests/test_sources.py` for Greenhouse normalization (`python -m pytest tests/test_sources.py::test_greenhouse_adapter_returns_active_internship_candidates_from_public_api -v`), then Lever retry behavior (`python -m pytest tests/test_sources.py::test_lever_adapter_retries_transient_failures_and_returns_internships_only -v`), and full suite: `python -m pytest tests -q` → `21 passed in 0.05s`. Next task: eligibility/location/season filtering with explicit rejection reasons.
- 2026-08-23 19:05 PDT — Completed task 5: extended `scanner.py` classification with explicit `rejection_reasons` for non-US/non-remote locations, non-target timelines, and sponsorship-required roles that violate Kevin's profile defaults. Verified via strict RED→GREEN in `tests/test_scanner.py` for location rejection (`python3 -m pytest tests/test_scanner.py::test_classify_rejects_non_us_location_with_explicit_reason -v`), timeline rejection (`python3 -m pytest tests/test_scanner.py::test_classify_rejects_non_target_season_with_explicit_reason -v`), sponsorship rejection (`python3 -m pytest tests/test_scanner.py::test_classify_rejects_sponsorship_required_roles_with_explicit_reason -v`), and full suite: `python3 -m pytest tests -q` → `24 passed in 0.04s`. Next task: concurrency locking so cron/manual runs cannot duplicate work.
- 2026-08-23 19:10 PDT — Completed task 6: added `RunLock` to `orchestrator.py` with a non-blocking file lock and `--lock-path` override so overlapping cron/manual runs fail fast before queue, audit, or report side effects. Verified via strict RED→GREEN in `tests/test_orchestrator.py` (`python3 -m pytest tests/test_orchestrator.py::test_main_exits_without_side_effects_when_lock_is_already_held -v`) and full suite: `python3 -m pytest tests -q` → `25 passed in 0.04s`. Next task: robust confirmation-evidence validation and fixtures.
- 2026-08-23 19:22 PDT — Completed task 7: tightened `pipeline.py` confirmation handling with HTML-to-text normalization plus success-phrase validation so pre-submit form fixtures are rejected while real confirmation fixtures are accepted. Verified via strict RED→GREEN in `tests/test_pipeline.py` for presubmit rejection (`python3 -m pytest tests/test_pipeline.py::test_submission_record_rejects_presubmit_greenhouse_fixture_text -v`), then Workday confirmation acceptance (`python3 -m pytest tests/test_pipeline.py::test_submission_record_accepts_workday_confirmation_fixture_text -v`), and full suite: `python3 -m pytest tests -q` → `26 passed in 0.05s`. Next task: browser/CDP health checks and recoverable error classification.
- 2026-08-23 19:26 PDT — Completed task 8: added `browser_health.py` with deterministic CDP readiness probing for `/json/version` and `/json/list`, recoverable error classification for connection-refused browser outages, and a CLI that emits JSON plus exit code `1` for recoverable health issues. Verified via strict RED→GREEN in `tests/test_browser_health.py` for ready-state probing (`python3 -m pytest tests/test_browser_health.py::test_probe_reports_ready_when_version_and_page_targets_exist -v`), then CLI recoverable-exit behavior (`python3 -m pytest tests/test_browser_health.py::test_main_returns_one_and_emits_json_for_recoverable_health_issue -v`), and full suite: `python3 -m pytest tests -q` → `30 passed in 0.04s`. Next task: safe tracker integration command that always cleans test rows.
- 2026-08-23 19:33 PDT — Completed task 9: extended `tracker.py` with a self-cleaning `integration-check` command that snapshots the live tracker, appends one marked smoke-test row, and restores the original rows even when append verification fails. Verified via strict RED→GREEN in `tests/test_tracker.py` for failure-path cleanup (`python3 -m pytest tests/test_tracker.py::test_run_integration_check_restores_original_rows_even_when_append_verification_fails -v`), then CLI wiring (`python3 -m pytest tests/test_tracker.py::test_main_integration_check_runs_smoke_command_and_prints_json -v`), tracker tests (`python3 -m pytest tests/test_tracker.py -q` → `5 passed in 0.02s`), and full suite: `python3 -m pytest tests -q` → `32 passed in 0.05s`. Next task: expand documentation, setup checks, operational runbooks, and security review.
- 2026-08-23 19:46 PDT — Completed task 10: added `setup_diagnostics.py` for offline readiness checks covering `profile.json`, `resume.primary`, Google Sheets OAuth token presence, and optional `--skip-browser` mode so local verification can pass without a live CDP session. Verified via strict RED→GREEN in `tests/test_setup_diagnostics.py` for blocking profile prerequisites (`python3 -m pytest tests/test_setup_diagnostics.py::test_run_checks_reports_blocking_profile_issues_without_touching_browser -v`), then offline CLI skip-browser behavior (`python3 -m pytest tests/test_setup_diagnostics.py::test_main_can_skip_browser_and_still_report_ready_for_offline_setup -v`), and full suite: `python3 -m pytest tests -q` → `34 passed in 0.06s`. Next task: expand operational runbooks and security review.
- 2026-08-23 20:00 PDT — Completed task 11: added `tests/test_repo_hygiene.py` to lock in ignore coverage for `runtime/` and `orchestrator-report.json`, fixed `.gitignore` so generated runtime artifacts stop appearing in `git status`, and added `OPERATIONS.md` plus README runbook guidance covering operator flow, incident triage, release checks, and the security review checklist. Verified via strict RED→GREEN in `tests/test_repo_hygiene.py` (`python3 -m pytest tests/test_repo_hygiene.py::test_gitignore_covers_runtime_and_report_artifacts -v`), then full suite: `python3 -m pytest tests -q` → `35 passed in 0.06s`. Next task: sprint queue complete; future work should start a new prioritized backlog.
- 2026-08-23 20:09 PDT — Completed task 12: added a token-driven `sources.py` CLI that merges Greenhouse and Lever public API results into one deterministic JSON artifact, strips tracking parameters from URLs, and deduplicates repeated postings across sources. Verified via strict RED→GREEN in `tests/test_sources.py` (`python3 -m pytest tests/test_sources.py::test_main_combines_source_tokens_deduplicates_urls_and_writes_candidates_json -v`), then full suite: `python3 -m pytest tests -q` → `36 passed in 0.06s`. Next task: add explicit source-fetch failure reporting so one bad token cannot hide partial collection results.
- 2026-08-23 20:10 PDT — Completed task 13: extended `sources.py` so per-token fetch failures are captured in an explicit `failures` array, successful candidates are still written to the output artifact, and the CLI exits `1` on partial collection instead of masking the error. Verified via strict RED→GREEN in `tests/test_sources.py` for partial-failure retention (`python3 -m pytest tests/test_sources.py::test_main_reports_failed_tokens_without_losing_successful_candidates -v`), sanity-checked the clean success path (`python3 -m pytest tests/test_sources.py::test_main_combines_source_tokens_deduplicates_urls_and_writes_candidates_json -v`), and ran the full suite: `python3 -m pytest tests -q` → `37 passed in 0.05s`. Next task: start a new prioritized backlog for post-sprint reliability work.
- 2026-08-23 20:16 PDT — Completed task 14: tightened `sources.py` configuration handling so a run with no `--greenhouse` or `--lever` tokens now fails fast with a machine-readable config error, exit code `2`, and no misleading empty candidates artifact. Verified via strict RED→GREEN in `tests/test_sources.py` (`python3 -m pytest tests/test_sources.py::test_main_requires_at_least_one_source_token -v`), then re-ran source tests (`python3 -m pytest tests/test_sources.py -q` → `5 passed in 0.02s`) and the full suite (`python3 -m pytest tests -q` → `38 passed in 0.05s`). Next task: add deterministic stale-result signaling when configured source tokens succeed but yield zero internship candidates.
- 2026-08-23 20:24 PDT — Completed task 15: extended `sources.py` so a zero-candidate run from otherwise successful configured source tokens now emits machine-readable stale-result signaling (`stale_result: true` + warning), preserves the deterministic empty JSON artifact, and exits `3` instead of silently returning success. Verified via strict RED→GREEN in `tests/test_sources.py` (`python3 -m pytest tests/test_sources.py::test_main_signals_zero_candidates_when_sources_return_no_internships -v`), then re-ran source tests (`python3 -m pytest tests/test_sources.py -q` → `6 passed in 0.01s`) and the full suite (`python3 -m pytest tests -q` → `39 passed in 0.05s`). Next task: start a new prioritized backlog for post-sprint reliability work.
- 2026-08-23 20:35 PDT — Completed task 16: extended `sources.py` stdout reporting with per-token `source_runs` status/count entries so automation can distinguish healthy zero-result boards from token-specific failures without diffing the output artifact. Verified via strict RED→GREEN in `tests/test_sources.py` (`python3 -m pytest tests/test_sources.py::test_main_reports_failed_tokens_without_losing_successful_candidates -v`), then re-ran source tests (`python3 -m pytest tests/test_sources.py -q` → `6 passed in 0.03s`) and the full suite (`python3 -m pytest tests -q` → `39 passed in 0.05s`). Next task: add a post-collection freshness signal based on newest posting timestamps so stale but non-empty public source snapshots are also detectable.
- 2026-08-23 20:40 PDT — Completed task 17: extended `sources.py` with timestamp parsing plus a `latest_posting_at` freshness signal so non-empty collections now emit `stale_result: true`, a 30-day freshness warning, and exit code `3` when their newest posting is too old. Verified via strict RED→GREEN in `tests/test_sources.py` (`python3 -m pytest tests/test_sources.py::test_main_signals_stale_non_empty_results_when_newest_posting_is_old -v`), then re-ran source tests (`python3 -m pytest tests/test_sources.py -q` → `7 passed in 0.02s`) and the full suite (`python3 -m pytest tests -q` → `40 passed in 0.05s`). Next task: start a new prioritized backlog for post-sprint reliability work.
- 2026-08-23 20:49 PDT — Completed task 18: extended `sources.py` per-token `source_runs` entries with `latest_posting_at` plus token-level stale warnings so one fresh board no longer hides another board's stale snapshot. Verified via strict RED→GREEN in `tests/test_sources.py` (`python3 -m pytest tests/test_sources.py::test_main_reports_per_token_freshness_when_one_source_is_stale -v`), then re-ran source tests (`python3 -m pytest tests/test_sources.py -q` → `8 passed in 0.03s`) and the full suite (`python3 -m pytest tests -q` → `41 passed in 0.04s`). Next task: add explicit per-token missing-timestamp signaling so freshness blind spots are machine-visible.
- 2026-08-23 20:58 PDT — Completed task 19: extended `sources.py` so successful source runs with non-empty results but no parseable posting timestamps now emit per-token `freshness_unknown: true` plus a machine-readable warning instead of silently looking fresh. Verified via strict RED→GREEN in `tests/test_sources.py` (`python3 -m pytest tests/test_sources.py::test_main_marks_source_run_when_posting_timestamps_are_missing -v`), then re-ran source tests (`python3 -m pytest tests/test_sources.py -q` → `9 passed in 0.03s`) and the full suite (`python3 -m pytest tests -q` → `42 passed in 0.05s`). Next task: surface an aggregate warning when any configured source succeeds without timestamps so orchestrators can fail closed on freshness blind spots.
- 2026-08-23 21:05 PDT — Completed task 20: extended `sources.py` so any successful source run missing posting timestamps now bubbles up to top-level stdout as `freshness_unknown: true`, a fail-closed aggregate warning, and exit code `3` instead of looking like a healthy collection. Verified via strict RED→GREEN in `tests/test_sources.py` (`python3 -m pytest tests/test_sources.py::test_main_fails_closed_when_any_source_run_is_missing_timestamps -v`), then re-ran source tests (`python3 -m pytest tests/test_sources.py -q` → `10 passed in 0.04s`) and the full suite (`python3 -m pytest tests -q` → `43 passed in 0.06s`). Next task: add an explicit aggregate freshness summary object/counts so downstream automation can branch on stale, unknown, and healthy source states without parsing warning strings.
- 2026-08-23 21:17 PDT — Completed task 21: extended `sources.py` stdout with an aggregate `freshness_summary` count object (`total_runs`, `healthy_runs`, `stale_runs`, `freshness_unknown_runs`, `error_runs`) so downstream automation can branch on source health without parsing warning strings, and documented it in `README.md`. Verified via strict RED→GREEN in `tests/test_sources.py` (`python3 -m pytest tests/test_sources.py::test_main_reports_aggregate_freshness_summary_counts -v`), then re-ran source tests (`python3 -m pytest tests/test_sources.py -q` → `11 passed in 0.04s`) and the full suite (`python3 -m pytest tests -q` → `44 passed in 0.07s`). Next task: add aggregate freshness bucket token lists so downstream automation can identify exactly which configured boards are healthy, stale, unknown, or errored without scanning `source_runs`.
- 2026-08-23 21:29 PDT — Completed task 22: extended `sources.py` stdout with aggregate `freshness_buckets` token lists for `healthy`, `stale`, `freshness_unknown`, and `error` states so downstream automation can identify exact affected boards without scanning `source_runs`, and documented it in `README.md`. Verified via strict RED→GREEN in `tests/test_sources.py` (`python3 -m pytest tests/test_sources.py::test_main_reports_freshness_bucket_tokens_for_each_source_status -v`), then re-ran source tests (`python3 -m pytest tests/test_sources.py -q` → `12 passed in 0.05s`) and the full suite (`python3 -m pytest tests -q` → `45 passed in 0.06s`). Next task: add an aggregate top-level source health status field so downstream automation can branch on `healthy`/`partial_error`/`stale_or_unknown` without re-deriving precedence rules.
- 2026-08-23 21:39 PDT — Completed task 23: extended `sources.py` stdout with a top-level `source_health_status` field that collapses source health into `healthy`, `partial_error`, or `stale_or_unknown`, including the zero-candidate stale path, and documented the new contract in `README.md`. Verified via strict RED→GREEN in `tests/test_sources.py` (`python3 -m pytest tests/test_sources.py::test_main_reports_top_level_source_health_status -v` → expected `KeyError: 'source_health_status'`, then pass), then verified the zero-candidate stale branch (`python3 -m pytest tests/test_sources.py::test_main_signals_zero_candidates_when_sources_return_no_internships -v`), re-ran source tests (`python3 -m pytest tests/test_sources.py -q` → `13 passed in 0.02s`), and ran the full suite (`python3 -m pytest tests -q` → `46 passed in 0.08s`). Next task: start a new prioritized backlog for post-sprint reliability work.
- 2026-08-23 21:47 PDT — Completed task 24: extended `sources.py` with an optional `--report` sidecar so the same machine-readable source-health payload emitted on stdout can also be persisted to disk for downstream automation, and documented it in `README.md`. Verified via strict RED→GREEN in `tests/test_sources.py` (`python3 -m pytest tests/test_sources.py::test_main_writes_report_artifact_matching_stdout_payload -v` → expected argparse failure on unknown `--report`, then pass), re-ran source tests (`python3 -m pytest tests/test_sources.py -q` → `14 passed in 0.03s`), and ran the full suite (`python3 -m pytest tests -q` → `47 passed in 0.05s`). Next task: teach `orchestrator.py` to consume a persisted source report and fail closed when upstream source health is `partial_error` or `stale_or_unknown`.
- 2026-08-23 22:01 PDT — Completed task 25: extended `orchestrator.py` with optional `--source-report` ingestion so dry runs now fail closed before queue/audit/report side effects unless the persisted `sources.py --report` payload says `source_health_status == "healthy"`, and documented the new guardrail in `README.md`. Verified via strict RED→GREEN in `tests/test_orchestrator.py` (`python3 -m pytest tests/test_orchestrator.py::test_main_fails_closed_when_source_report_is_not_healthy -v` → expected argparse `unrecognized arguments: --source-report`, then pass) and full suite (`python3 -m pytest tests -q` → `48 passed in 0.06s`). Next task: validate `--source-report` schema explicitly so malformed or missing `source_health_status` sidecars also fail closed with a stable machine-readable reason.
- 2026-08-23 22:00 PDT — Completed task 26: tightened `orchestrator.py` source-report validation so malformed JSON and sidecars missing `source_health_status` now fail closed with stable reasons (`invalid_json`, `missing_source_health_status`), plus explicit schema/status validation for future bad sidecars, and documented the contract in `README.md`. Verified via strict RED→GREEN in `tests/test_orchestrator.py` for malformed JSON (`python3 -m pytest tests/test_orchestrator.py::test_main_fails_closed_with_stable_reason_when_source_report_is_malformed_json -v` → expected raw `JSONDecodeError`, then pass), then missing health status (`python3 -m pytest tests/test_orchestrator.py::test_main_fails_closed_with_stable_reason_when_source_report_omits_health_status -v` → expected `Source health check failed: None`, then pass), and full suite (`python3 -m pytest tests -q` → `50 passed in 0.06s`). Next task: add a verified CLI regression for `invalid_source_health_status` so unexpected upstream enum drift also stays fail-closed.
- 2026-08-23 22:08 PDT — Completed task 27: tightened `orchestrator.py` source-report loading so a missing or otherwise unreadable `--source-report` path now fails closed with the stable reason `Invalid source report: unreadable` instead of leaking raw `FileNotFoundError`, and documented the contract in `README.md`. Verified via strict RED→GREEN in `tests/test_orchestrator.py` (`python3 -m pytest tests/test_orchestrator.py::test_main_fails_closed_with_stable_reason_when_source_report_path_is_missing -v` → expected raw `FileNotFoundError`, then pass) and full suite (`python3 -m pytest tests -q` → `51 passed in 0.07s`). Next task: add a verified CLI regression for `invalid_source_health_status` so unexpected upstream enum drift also stays fail-closed.
- 2026-08-23 22:13 PDT — Completed task 28: tightened `orchestrator.py` source-report validation so non-string or otherwise malformed `source_health_status` values (for example an upstream list instead of an enum string) now fail closed with the stable reason `Invalid source report: invalid_source_health_status` instead of leaking a raw `TypeError`. Verified via strict RED→GREEN in `tests/test_orchestrator.py` (`python3 -m pytest tests/test_orchestrator.py::test_main_fails_closed_with_stable_reason_when_source_report_status_is_not_a_string -v` → expected `TypeError: unhashable type: 'list'`, then pass) and full suite (`python3 -m pytest tests -q` → `52 passed in 0.06s`). Next task: start a new prioritized backlog for post-sprint reliability work.
- 2026-08-23 22:19 PDT — Completed task 29: tightened `orchestrator.py` source-report validation so a payload that claims `source_health_status == "healthy"` but still contains non-empty `failures` now fails closed with the stable reason `Invalid source report: inconsistent_source_health` before any queue, audit, or report side effects; documented the new guardrail in `README.md`. Verified via strict RED→GREEN in `tests/test_orchestrator.py` (`python3 -m pytest tests/test_orchestrator.py::test_main_fails_closed_when_healthy_source_report_contains_failures -v` → expected `Failed: DID NOT RAISE <class 'SystemExit'>`, then pass) and full suite (`python3 -m pytest tests -q` → `53 passed in 0.07s`). Next task: fail closed when a supposedly healthy source report contains non-OK `source_runs` entries so upstream status drift cannot hide stale or errored boards.
- 2026-08-23 22:44 PDT — Completed task 30: tightened `orchestrator.py` source-report validation so a payload that claims `source_health_status == "healthy"` but contains non-OK `source_runs` entries (stale, freshness-unknown, or errored boards) now fails closed with the stable reason `Invalid source report: inconsistent_source_health`; documented the guardrail in `README.md`. Verified via strict RED→GREEN in `tests/test_orchestrator.py` (`python3 -m pytest tests/test_orchestrator.py::test_main_fails_closed_when_healthy_source_report_contains_non_ok_source_run -v` → expected `Failed: DID NOT RAISE <class 'SystemExit'>`, then pass) and full suite (`python3 -m pytest tests -q` → `54 passed in 0.06s`). Next task: validate `freshness_summary` consistency so a supposedly healthy source report cannot hide contradictory aggregate counts.
- 2026-08-23 23:21 PDT — Completed task 31: tightened `orchestrator.py` source-report validation so a payload that claims `source_health_status == "healthy"` but carries a contradictory `freshness_summary` count object now fails closed with the stable reason `Invalid source report: inconsistent_source_health`; documented the guardrail in `README.md`. Verified via strict RED→GREEN in `tests/test_orchestrator.py` (`python3 -m pytest tests/test_orchestrator.py::test_main_fails_closed_when_healthy_source_report_has_contradictory_freshness_summary -v` → expected `Failed: DID NOT RAISE <class 'SystemExit'>`, then pass) and full suite (`python3 -m pytest tests -q` → `55 passed in 0.06s`). Next task: validate `freshness_buckets` consistency so a supposedly healthy source report cannot hide contradictory per-token bucket membership.
- 2026-08-24 00:01 PDT — Completed task 32: tightened `orchestrator.py` source-report validation so a payload that claims `source_health_status == "healthy"` but carries contradictory `freshness_buckets` token membership now fails closed with the stable reason `Invalid source report: inconsistent_source_health`; documented the guardrail in `README.md`. Verified via strict RED→GREEN in `tests/test_orchestrator.py` (`python3 -m pytest tests/test_orchestrator.py::test_main_fails_closed_when_healthy_source_report_has_contradictory_freshness_buckets -v` → expected `Failed: DID NOT RAISE <class 'SystemExit'>`, then pass) and full suite (`python3 -m pytest tests -q` → `56 passed in 0.06s`). Next task: validate that `source_runs` and aggregate freshness artifacts fail closed when duplicated `{source, token}` entries create ambiguous health membership.
- 2026-08-23 23:39 PDT — Completed task 33: tightened `orchestrator.py` source-report validation so a payload that claims `source_health_status == "healthy"` but repeats the same `{source, token}` identity in `source_runs` now fails closed with the stable reason `Invalid source report: inconsistent_source_health` instead of accepting ambiguous per-board health. Verified via strict RED→GREEN in `tests/test_orchestrator.py` (`python3 -m pytest tests/test_orchestrator.py::test_main_fails_closed_when_healthy_source_report_duplicates_source_run_identity -v` → expected `Failed: DID NOT RAISE <class 'SystemExit'>`, then pass) and full suite (`python3 -m pytest tests -q` → `57 passed in 0.21s`). Next task: validate that duplicated `{source, token}` entries hidden inside `freshness_buckets` also fail closed even when `source_runs` itself is unique.
- 2026-08-23 23:48 PDT — Completed task 34: tightened `orchestrator.py` source-report validation so a payload that claims `source_health_status == "healthy"` but omits the required `source_runs` list now fails closed with the stable reason `Invalid source report: invalid_schema` instead of being accepted without any per-board evidence; documented the guardrail in `README.md`. Verified via strict RED→GREEN in `tests/test_orchestrator.py` (`python3 -m pytest tests/test_orchestrator.py::test_main_fails_closed_when_healthy_source_report_omits_source_runs -v` → expected `Failed: DID NOT RAISE <class 'SystemExit'>`, then pass) and full suite (`python3 -m pytest tests -q` → `58 passed in 0.06s`). Next task: require `freshness_summary` and `freshness_buckets` on healthy source reports so aggregate health evidence cannot be omitted while still claiming `healthy`.
- 2026-08-24 00:15 PDT — Completed task 35: tightened `orchestrator.py` source-report validation so a payload that claims `source_health_status == "healthy"` but omits either required aggregate artifact (`freshness_summary` or `freshness_buckets`) now fails closed with the stable reason `Invalid source report: invalid_schema`; updated `README.md` and backfilled affected regression fixtures so older inconsistency tests still exercise the intended fail-closed paths. Verified via strict RED→GREEN in `tests/test_orchestrator.py` (`python3 -m pytest tests/test_orchestrator.py::test_main_fails_closed_when_healthy_source_report_omits_required_freshness_artifact -v` → expected `Failed: DID NOT RAISE <class 'SystemExit'>`, then pass), spot-checked preserved inconsistency regressions (`python3 -m pytest tests/test_orchestrator.py::test_main_fails_closed_when_healthy_source_report_contains_failures -v`, `...contains_non_ok_source_run -v`, `...has_contradictory_freshness_summary -v`, `...has_contradictory_freshness_buckets -v`), and ran the full suite (`python3 -m pytest tests -q` → `60 passed in 0.06s`). Next task: validate `source_runs` entries themselves on healthy source reports so malformed per-board records fail closed even when aggregate artifacts are present.
- 2026-08-24 00:28 PDT — Completed task 36: tightened `orchestrator.py` source-report validation so a payload that claims `source_health_status == "healthy"` but contains malformed `source_runs` entries now fails closed with the stable reason `Invalid source report: invalid_schema`; documented the contract in `README.md`. Verified via strict RED→GREEN in `tests/test_orchestrator.py` (`python3 -m pytest tests/test_orchestrator.py::test_main_fails_closed_when_healthy_source_report_contains_malformed_source_run_entry -v` → expected `Failed: DID NOT RAISE <class 'SystemExit'>`, then pass) and full suite (`python3 -m pytest tests -q` → `61 passed in 0.07s`). Next task: validate `freshness_buckets` entries themselves on healthy source reports so malformed aggregate token identities also fail closed.
- 2026-08-24 00:14 PDT — Completed task 37: tightened `orchestrator.py` source-report validation so a payload that claims `source_health_status == "healthy"` but contains malformed `freshness_buckets` entries now fails closed with the stable reason `Invalid source report: invalid_schema` instead of being misclassified as inconsistent health. Verified via strict RED→GREEN in `tests/test_orchestrator.py` (`python3 -m pytest tests/test_orchestrator.py::test_main_fails_closed_when_healthy_source_report_contains_malformed_freshness_bucket_entry -v` → expected `Invalid source report: inconsistent_source_health`, then pass) and full suite (`python3 -m pytest tests -q` → `62 passed in 0.09s`). Next task: validate `freshness_summary` entries themselves on healthy source reports so malformed aggregate count types also fail closed with `invalid_schema`.
- 2026-08-24 00:23 PDT — Completed task 38: tightened `orchestrator.py` source-report validation so a payload that claims `source_health_status == "healthy"` but contains malformed `freshness_summary` entries now fails closed with the stable reason `Invalid source report: invalid_schema` instead of being misclassified as inconsistent health; documented the contract in `README.md`. Verified via strict RED→GREEN in `tests/test_orchestrator.py` (`python3 -m pytest tests/test_orchestrator.py::test_main_fails_closed_when_healthy_source_report_contains_malformed_freshness_summary_entry -v` → expected `Invalid source report: inconsistent_source_health`, then pass) and full suite (`python3 -m pytest tests -q` → `63 passed in 0.06s`). Next task: validate top-level `failures` entries on healthy source reports so malformed upstream failure payloads fail closed with `invalid_schema` before health consistency checks.
- 2026-08-24 00:31 PDT — Completed task 39: tightened `orchestrator.py` source-report validation so a payload that claims `source_health_status == "healthy"` but contains malformed top-level `failures` entries now fails closed with the stable reason `Invalid source report: invalid_schema` before health consistency checks; documented the contract in `README.md`. Verified via strict RED→GREEN in `tests/test_orchestrator.py` (`python3 -m pytest tests/test_orchestrator.py::test_main_fails_closed_when_healthy_source_report_contains_malformed_failure_entry -v` → expected `Invalid source report: inconsistent_source_health`, then pass) and full suite (`python -m pytest tests -q` → `64 passed in 0.07s`). Next task: validate that a supposedly healthy source report cannot also set top-level `stale_result` or `freshness_unknown` aggregate flags.
- 2026-08-24 00:38 PDT — Completed task 40: tightened `orchestrator.py` source-report validation so a payload that claims `source_health_status == "healthy"` but still sets top-level aggregate stale/freshness-unknown flags now fails closed with the stable reason `Invalid source report: inconsistent_source_health`; documented the contract in `README.md`. Verified via strict RED→GREEN in `tests/test_orchestrator.py` (`python3 -m pytest tests/test_orchestrator.py::test_main_fails_closed_when_healthy_source_report_sets_top_level_stale_result -v` → expected `Failed: DID NOT RAISE <class 'SystemExit'>`, then pass) and full suite (`python3 -m pytest tests -q` → `65 passed in 0.08s`). Next task: validate schema for optional top-level aggregate freshness flags so malformed non-boolean values also fail closed deterministically.
- 2026-08-24 00:50 PDT — Completed task 41: tightened `orchestrator.py` source-report validation so optional top-level aggregate freshness flags (`stale_result`, `freshness_unknown`) must be booleans when present on a `healthy` source report, otherwise the run fails closed with the stable reason `Invalid source report: invalid_schema`; documented the contract in `README.md`. Verified via strict RED→GREEN in `tests/test_orchestrator.py` (`python3 -m pytest tests/test_orchestrator.py::test_main_fails_closed_when_healthy_source_report_has_non_boolean_top_level_freshness_flag -v` → expected `Invalid source report: inconsistent_source_health`, then pass) and full suite (`python -m pytest tests -q` → `67 passed in 0.07s`). Next task: validate schema for optional top-level aggregate warning text so malformed non-string warning values fail closed deterministically.
- 2026-08-24 00:58 PDT — Completed task 42: tightened `orchestrator.py` source-report validation so optional top-level aggregate `warning` text must be a string when present on a `healthy` source report, otherwise the run fails closed with the stable reason `Invalid source report: invalid_schema`; documented the contract in `README.md`. Verified via strict RED→GREEN in `tests/test_orchestrator.py` (`python -m pytest tests/test_orchestrator.py::test_main_fails_closed_when_healthy_source_report_has_non_string_top_level_warning -v` → expected `Failed: DID NOT RAISE <class 'SystemExit'>`, then pass) and full suite (`python -m pytest tests -q` → `68 passed in 0.10s`). Next task: validate schema for optional top-level aggregate `latest_posting_at` so malformed non-string timestamps fail closed deterministically.
- 2026-08-24 01:07 PDT — Completed task 43: tightened `orchestrator.py` source-report validation so optional top-level aggregate `latest_posting_at` must be a string when present on a `healthy` source report, otherwise the run fails closed with the stable reason `Invalid source report: invalid_schema`; documented the contract in `README.md`. Verified via strict RED→GREEN in `tests/test_orchestrator.py` (`python -m pytest tests/test_orchestrator.py::test_main_fails_closed_when_healthy_source_report_has_non_string_top_level_latest_posting_at -v` → expected `Failed: DID NOT RAISE <class 'SystemExit'>`, then pass) and full suite (`python -m pytest tests -q` → `69 passed in 0.07s`). Next task: validate schema for top-level `failures` itself on healthy source reports so a non-list failures payload fails closed deterministically as `invalid_schema`.
- 2026-08-24 01:16 PDT — Completed task 44: tightened `orchestrator.py` source-report validation so optional top-level aggregate `latest_posting_at` must be a parseable ISO-8601 timestamp string when present on a `healthy` source report, otherwise the run fails closed with the stable reason `Invalid source report: invalid_schema`; documented the contract in `README.md`. Verified via strict RED→GREEN in `tests/test_orchestrator.py` (`python -m pytest tests/test_orchestrator.py::test_main_fails_closed_when_healthy_source_report_has_unparseable_top_level_latest_posting_at -v` → expected `Failed: DID NOT RAISE <class 'SystemExit'>`, then pass) and full suite (`python -m pytest tests -q` → `70 passed in 0.07s`). Next task: validate healthy `source_runs[*].latest_posting_at` values so unparseable per-source timestamps also fail closed as `invalid_schema`.
- 2026-08-24 01:25 PDT — Completed task 45: tightened `orchestrator.py` source-report validation so `healthy` `source_runs[*].latest_posting_at` values must be parseable ISO-8601 timestamp strings, otherwise the run fails closed with the stable reason `Invalid source report: invalid_schema`; documented the contract in `README.md`. Verified via strict RED→GREEN in `tests/test_orchestrator.py` (`python3 -m pytest tests/test_orchestrator.py::test_main_fails_closed_when_healthy_source_report_has_unparseable_source_run_latest_posting_at -v` → expected `Failed: DID NOT RAISE <class 'SystemExit'>`, then pass) and full suite (`python3 -m pytest tests -q` → `71 passed in 0.08s`). Next task: validate that healthy top-level `latest_posting_at` stays consistent with per-source `source_runs[*].latest_posting_at` evidence so aggregate freshness cannot drift from the underlying board timestamps.
- 2026-08-24 01:33 PDT — Completed task 46: tightened `orchestrator.py` source-report validation so a `healthy` top-level `latest_posting_at` must exactly match the newest `source_runs[*].latest_posting_at` evidence, otherwise the run fails closed with the stable reason `Invalid source report: inconsistent_source_health`; documented the contract in `README.md`. Verified via strict RED→GREEN in `tests/test_orchestrator.py` (`python3 -m pytest tests/test_orchestrator.py::test_main_fails_closed_when_healthy_source_report_top_level_latest_posting_at_disagrees_with_source_runs -v` → expected `Failed: DID NOT RAISE <class 'SystemExit'>`, then pass) and full suite (`python3 -m pytest tests -q` → `72 passed in 0.08s`). Next task: validate that `healthy` reports cannot provide a top-level `latest_posting_at` when none of the underlying `source_runs[*].latest_posting_at` values exist.
- 2026-08-24 01:40 PDT — Completed task 47: fixed `orchestrator.py` source-report validation so `healthy` top-level `latest_posting_at` comparison now uses parsed datetimes instead of raw string ordering, which keeps timezone-offset source runs from being rejected when they still point to the true newest posting. Verified via strict RED→GREEN in `tests/test_orchestrator.py` (`python3 -m pytest tests/test_orchestrator.py::test_main_accepts_healthy_source_report_when_top_level_latest_posting_at_matches_newest_source_run_across_timezones -v` → expected `SystemExit: Invalid source report: inconsistent_source_health`, then pass) and full suite (`python3 -m pytest tests -q` → `73 passed in 0.07s`). Next task: validate that mixed naive/offset-aware `latest_posting_at` timestamps fail closed with a stable reason instead of raising a raw datetime comparison error.
- 2026-08-24 01:45 PDT — Completed task 48: tightened `orchestrator.py` source-report validation so `healthy` reports that mix naive and offset-aware `latest_posting_at` timestamps now fail closed with the stable reason `Invalid source report: invalid_schema` instead of raising a raw datetime comparison `TypeError`. Verified via strict RED→GREEN in `tests/test_orchestrator.py` (`python3 -m pytest tests/test_orchestrator.py::test_main_fails_closed_when_healthy_source_report_mixes_naive_and_offset_aware_latest_posting_at -v` → expected `TypeError: can't compare offset-naive and offset-aware datetimes`, then pass) and full suite (`python3 -m pytest tests -q` → `74 passed in 0.08s`). Next task: validate that `healthy` reports cannot provide a top-level `latest_posting_at` when none of the underlying `source_runs[*].latest_posting_at` values exist.
- 2026-08-24 02:00 PDT — Completed task 49: fixed `orchestrator.py` source-report validation so `healthy` top-level `latest_posting_at` is compared by parsed timestamp value instead of exact string formatting, which accepts semantically equivalent ISO-8601 representations like `Z` vs `+00:00` while still rejecting stale aggregate drift. Verified via strict RED→GREEN in `tests/test_orchestrator.py` (`python3 -m pytest tests/test_orchestrator.py::test_main_accepts_healthy_source_report_when_top_level_latest_posting_at_matches_equivalent_newest_source_run_timestamp -v` → expected `SystemExit: Invalid source report: inconsistent_source_health`, then pass), regression-checking the mismatch path (`python3 -m pytest tests/test_orchestrator.py::test_main_fails_closed_when_healthy_source_report_top_level_latest_posting_at_disagrees_with_source_runs -v`), and full suite (`python3 -m pytest tests -q` → `75 passed in 0.08s`). Next task: validate that `healthy` reports cannot provide a top-level `latest_posting_at` when none of the underlying `source_runs[*].latest_posting_at` values exist.
- 2026-08-24 02:02 PDT — Completed task 50: tightened `orchestrator.py` source-report validation so a payload that claims `source_health_status == "healthy"` but hides a missing `source_runs[*].latest_posting_at` value now fails closed with the stable reason `Invalid source report: inconsistent_source_health` instead of being accepted as fully fresh. Verified via strict RED→GREEN in `tests/test_orchestrator.py` (`python3 -m pytest tests/test_orchestrator.py::test_main_fails_closed_when_healthy_source_report_hides_missing_source_run_latest_posting_at -v` → expected `Failed: DID NOT RAISE <class 'SystemExit'>`, then pass) and full suite (`python3 -m pytest tests -q` → `76 passed in 0.09s`). Next task: validate that `healthy` reports cannot omit top-level `latest_posting_at` when `source_runs[*].latest_posting_at` evidence exists.
- 2026-08-24 02:10 PDT — Completed task 51: tightened `orchestrator.py` source-report validation so a payload that claims `source_health_status == "healthy"` but omits top-level `latest_posting_at` despite healthy `source_runs[*].latest_posting_at` evidence now fails closed with the stable reason `Invalid source report: inconsistent_source_health`. Verified via strict RED→GREEN in `tests/test_orchestrator.py` (`python3 -m pytest tests/test_orchestrator.py::test_main_fails_closed_when_healthy_source_report_omits_top_level_latest_posting_at_despite_source_run_evidence -v` → expected `Failed: DID NOT RAISE <class 'SystemExit'>`, then pass), regression-checked the equivalent-timestamp success path (`python3 -m pytest tests/test_orchestrator.py::test_main_accepts_healthy_source_report_when_top_level_latest_posting_at_matches_equivalent_newest_source_run_timestamp -v`), and ran the full suite (`python -m pytest tests -q` → `77 passed in 0.08s`). Next task: validate that `healthy` reports cannot claim aggregate freshness metadata (`warning`, `stale_result`, `freshness_unknown`) when `source_runs` is an empty but otherwise schema-valid list.
- 2026-08-24 02:18 PDT — Completed task 52: tightened `orchestrator.py` source-report validation so a payload that claims `source_health_status == "healthy"` but provides an empty `source_runs` list while still claiming aggregate freshness metadata (`warning`, `stale_result`, or `freshness_unknown`) now fails closed with the stable reason `Invalid source report: inconsistent_source_health`; documented the contract in `README.md`. Verified via strict RED→GREEN in `tests/test_orchestrator.py` (`python3 -m pytest tests/test_orchestrator.py::test_main_fails_closed_when_empty_healthy_source_report_claims_aggregate_freshness_metadata -v` → expected `Failed: DID NOT RAISE <class 'SystemExit'>`, then pass) and full suite (`python -m pytest tests -q` → `78 passed in 0.08s`). Next task: validate that `healthy` reports cannot use an empty `source_runs` list at all, even when aggregate freshness metadata is omitted.
- 2026-08-24 16:45 PDT — Completed task 1 from the new backlog: added an American Express regression in `tests/test_scanner.py` for an active year-only `2027 Software Engineer, Technology` title without an explicit season, verified RED with `python -m pytest tests/test_scanner.py::test_classify_accepts_active_american_express_style_2027_title_without_explicit_season -v` failing on `assert False is True`, then GREEN by teaching `scanner.relevant()`/`rejection_reasons()` to consider auxiliary internship text from `season` when the normalized role drops the internship keyword. Re-ran scanner tests (`python -m pytest tests/test_scanner.py -q` → `10 passed in 0.01s`) and the full suite (`python -m pytest tests -q` → `79 passed in 0.09s`). Next task: structured question-answer engine with profile + Kevin Bible precedence and fail-closed conflicts.
- 2026-08-24 17:00 PDT — Completed task 2 from the new backlog: added `question_engine.py` plus `tests/test_question_engine.py` as a structured answer resolver that prefers confirmed profile facts over conflicting Google Doc values, prefers company-specific Google Doc answers over generic entries, fails closed when a question needs an unknown exact fact (for example HNRC start month), and returns a deterministic conflict result when equally specific Google Doc answers disagree. Verified strict RED→GREEN on each slice with `python -m pytest tests/test_question_engine.py::test_answer_uses_profile_fact_before_conflicting_google_doc_value -v` (initially `ModuleNotFoundError: No module named 'question_engine'`), `python -m pytest tests/test_question_engine.py::test_answer_prefers_company_specific_google_doc_entry_over_generic_one -v` (initially `assert 'unknown' == 'answered'`), `python -m pytest tests/test_question_engine.py::test_answer_fails_closed_when_exact_start_month_is_unknown -v` (initially `assert 'unknown_question' == 'unknown_profile_fact'`), and `python -m pytest tests/test_question_engine.py::test_answer_fails_closed_when_company_specific_google_doc_answers_conflict -v` (initially `assert 'answered' == 'conflict'`). Re-ran the full suite: `python -m pytest tests -q` → `83 passed in 0.09s`. Next task: executable Lever handler with field inventory, file upload, read-back verification, manual-gate detection, confirmation validation, and fixtures.
- 2026-08-24 17:09 PDT — Completed task 3 from the new backlog: added `lever_handler.py` plus `tests/test_lever_handler.py` and harmless Lever fixtures for application/manual-gate/confirmation pages so the handler now inventories visible fields, verifies uploaded resume filenames by read-back, detects CAPTCHA-style manual gates, reuses confirmation evidence validation, and exposes a machine-readable CLI. Verified strict RED→GREEN on each slice with `python -m pytest tests/test_lever_handler.py::test_inspect_application_fixture_inventories_fields_and_verifies_uploaded_resume -v` (initially `ModuleNotFoundError: No module named 'lever_handler'`), `python -m pytest tests/test_lever_handler.py::test_inspect_manual_gate_fixture_detects_captcha_blocker -v` (after trimming premature code, initially `assert None == {'type': 'captcha', 'detail': 'CAPTCHA detected'}`), `python -m pytest tests/test_lever_handler.py::test_inspect_confirmation_fixture_uses_verified_confirmation_evidence -v` (initially `assert 'application' == 'confirmation'`), and `python -m pytest tests/test_lever_handler.py::test_main_emits_machine_readable_json_for_fixture -v` (initially `AttributeError: module 'lever_handler' has no attribute 'main'`). Re-ran Lever tests (`python -m pytest tests/test_lever_handler.py -q` → `4 passed in 0.01s`) and the full suite (`python -m pytest tests -q` → `87 passed in 0.09s`). Next task: Oracle Recruiting handler with real combobox option selection, country validation, salary dropdown handling, issue navigation, and fixtures.
- 2026-08-24 17:18 PDT — Completed task 4 from the new backlog: added `oracle_handler.py` plus `tests/test_oracle_handler.py` and harmless Oracle fixtures for application/issues/confirmation pages so the handler now inventories Oracle-style combobox fields with selected options, verifies uploaded resume filenames by read-back, validates `United States` country selection, surfaces salary dropdown state, exposes issue-navigation targets from validation summaries, reuses confirmation evidence validation, and emits machine-readable CLI JSON. Verified strict RED→GREEN on each slice with `python -m pytest tests/test_oracle_handler.py::test_inspect_application_fixture_inventories_fields_and_selected_options -v` (initially `ModuleNotFoundError: No module named 'oracle_handler'`), `python -m pytest tests/test_oracle_handler.py::test_inspect_issue_fixture_reports_navigation_targets_and_invalid_country -v` (initially `assert [] == [{'message': 'Country must be set to United States.', 'target': 'country'}, {'message': 'Please choose a salary expectation.', 'target': 'salary'}]`), and `python -m pytest tests/test_oracle_handler.py::test_inspect_confirmation_fixture_uses_verified_confirmation_evidence -v` (initially `assert 'application' == 'confirmation'`). Re-ran Oracle tests (`python -m pytest tests/test_oracle_handler.py -q` → `4 passed in 0.01s`) and the full suite (`python -m pytest tests -q` → `91 passed in 0.10s`). Next task: extend the SQLite queue worker with leases, attempt counts, retry backoff, pending-question/CAPTCHA/approval states, terminal failures, and stale-lease recovery.
- 2026-08-24 17:31 PDT — Completed task 5 from the new backlog: extended `app_queue.py` into a lease-aware queue worker with persisted `attempt_count`, `available_at`, `lease_expires_at`, and `last_error` fields; added `lease_next()` + `finish_lease()` so jobs can be leased, retried with backoff, recovered after stale leases expire, and moved through manual-gate states before being requeued. Verified strict RED→GREEN with `python -m pytest tests/test_app_queue.py::test_lease_next_claims_available_job_and_increments_attempt_count -v` (initially `AttributeError: 'ApplicationQueue' object has no attribute 'lease_next'`), `python -m pytest tests/test_app_queue.py::test_finish_lease_retry_applies_backoff_and_preserves_error_detail -v` (initially `AttributeError: 'ApplicationQueue' object has no attribute 'finish_lease'`), `python -m pytest tests/test_app_queue.py::test_lease_next_recovers_stale_lease_after_expiration -v` (initially `assert None is not None`), and `python -m pytest tests/test_app_queue.py::test_pending_approval_job_can_be_requeued_for_a_new_attempt -v` (initially `ValueError: Invalid transition: pending_approval -> discovered`). Re-ran queue tests (`python -m pytest tests/test_app_queue.py -q` → `7 passed in 0.02s`) and the full suite (`python -m pytest tests -q` → `95 passed in 0.13s`). Next task: wire deterministic source collection → health report → scanner → orchestrator → queue into one production-safe command.
- 2026-08-24 22:23 PDT — Completed task 6 from the new backlog: added `production_run.py` plus `tests/test_production_run.py` as a production-safe dry-run entrypoint that runs `sources.py`, persists `candidates.json` + `sources-report.json`, passes the verified health sidecar into `orchestrator.py`, persists `orchestrator-report.json`, and emits one machine-readable summary with both exit codes. Verified strict RED→GREEN with `python3 -m pytest tests/test_production_run.py::test_main_runs_sources_then_orchestrator_and_persists_all_artifacts -v` (initially `ModuleNotFoundError: No module named 'production_run'`), then re-ran the full suite (`python3 -m pytest tests -q` → `96 passed in 0.32s`). Next task: Discord job-ID-bound approval/reject/retry/skip controls without changing the postponed token-rotation policy.
- 2026-08-24 22:34 PDT — Completed task 7 from the new backlog: added `discord_controls.py` plus `tests/test_discord_controls.py` so the queue now has job-ID-bound `approve`/`reject` controls for `pending_approval` jobs, `retry`/`skip` controls for `pending_question` and `pending_captcha` jobs, state-gated validation, per-job control discovery, and a machine-readable CLI; documented the contract in `README.md`. Verified strict RED→GREEN with `python3 -m pytest tests/test_discord_controls.py::test_approve_control_requeues_matching_pending_approval_job -v` (initially `ModuleNotFoundError: No module named 'discord_controls'`), `python3 -m pytest tests/test_discord_controls.py::test_reject_control_marks_matching_pending_approval_job_failed -v` (initially `ValueError: Unknown control action: reject`), `python3 -m pytest tests/test_discord_controls.py::test_retry_control_requeues_matching_pending_question_job -v` (initially `ValueError: Unknown control action: retry`), and `python3 -m pytest tests/test_discord_controls.py::test_controls_for_job_returns_only_actions_valid_for_its_state -v` (initially `AttributeError: module 'discord_controls' has no attribute 'controls_for_job'`). Re-ran the focused control suite (`python3 -m pytest tests/test_discord_controls.py -q` → `7 passed in 0.03s`) and the full suite (`python3 -m pytest tests -q` → `103 passed in 0.14s`). Next task: live non-submitting ATS preflight tests against Greenhouse, Workday, Lever, and Oracle forms.
- 2026-08-24 22:50 PDT — Progress on task 8: added `ats_preflight.py` plus `tests/test_ats_preflight.py` as a snapshot-driven non-submitting preflight runner over Greenhouse, Workday, Lever, and Oracle HTML captures. Verified strict RED→GREEN with `python3 -m pytest tests/test_ats_preflight.py::test_run_preflight_manifest_inspects_supported_fixture_pages -v` (initially `ModuleNotFoundError: No module named 'ats_preflight'`, then `AssertionError` on Greenhouse select-label parsing, then pass), re-ran the focused suite (`python3 -m pytest tests/test_ats_preflight.py -q` → `1 passed in 0.01s`), and re-ran the full suite (`python3 -m pytest tests -q` → `104 passed in 0.14s`). Live non-submitting snapshot run via `python3 ats_preflight.py /Users/kevinpoopz/.hermes/cache/browser-use/workspace/1697b548-5cc6-42ec-a633-4204e0b59a70/live_preflight_manifest.json` confirmed a real Greenhouse application page, a real Lever application page with CAPTCHA/manual-gate detection, an active Oracle internship posting with `APPLY NOW`, and a live Tencent Workday posting after cookie acceptance; task 8 remains open because direct Workday/Oracle application-surface parsing still needs a browser-assisted path beyond the current HTML snapshot parser.
- 2026-08-24 23:09 PDT — Completed task 8 from the new backlog: extended `ats_preflight.py` and `oracle_handler.py` so live-style Workday and Oracle rendered entrypoints are classified as `listing` surfaces with verified apply/sign-in metadata instead of being misread as blank applications, backed by new sanitized live-entrypoint fixtures in `tests/test_ats_preflight.py`. Verified strict RED→GREEN with `python3 -m pytest tests/test_ats_preflight.py::test_inspect_workday_live_entrypoint_fixture_detects_rendered_apply_surface -v` (first failed on `assert 'application' == 'listing'`, then on `assert 'Careers' == 'Software Engineering Intern'`, then passed) and `python3 -m pytest tests/test_ats_preflight.py::test_inspect_oracle_live_entrypoint_fixture_detects_apply_button_without_false_form_fields -v` (initially failed on `assert 'application' == 'listing'`, then passed). Re-ran focused suites (`python3 -m pytest tests/test_ats_preflight.py -q` → `3 passed in 0.01s`, `python3 -m pytest tests/test_oracle_handler.py -q` → `4 passed in 0.01s`) and full suite (`python3 -m pytest tests -q` → `106 passed in 0.15s`). Live non-submitting verification via `python3 ats_preflight.py /Users/kevinpoopz/.hermes/cache/browser-use/workspace/1697b548-5cc6-42ec-a633-4204e0b59a70/live_preflight_manifest_rendered.json` now reports the Tencent Workday rendered surface as `listing` with `Apply` + `Sign In` + requisition `R107162`, the Oracle OVIP page as `listing` with `Apply Now`, Greenhouse as an application form, and Lever as an application form with a CAPTCHA manual gate. Next task: sanitized submission-evidence artifacts and tracker/Discord reconciliation.
- 2026-08-24 23:12 PDT — Progress on task 9: added `submission_artifacts.py` plus `tests/test_submission_artifacts.py` so verified submission evidence can now be emitted as a sanitized artifact that reuses `pipeline.submission_row(...)`, redacts emails/phone numbers from normalized confirmation excerpts, and records a single reconciliation object tying the tracker status to the Discord `applied` notification payload. Verified strict RED→GREEN with `python3 -m pytest tests/test_submission_artifacts.py::test_build_submission_artifact_sanitizes_confirmation_html_and_reconciles_tracker_with_discord_message -v` (initially `ModuleNotFoundError: No module named 'submission_artifacts'`, then passed) and re-ran the full suite (`python3 -m pytest tests -q` → `107 passed in 0.15s`). Next task: extend task 9 from artifact generation into read-back reconciliation against concrete tracker append / Discord delivery results.
- 2026-08-24 23:24 PDT — Completed task 9 from the new backlog: extended `submission_artifacts.py` so sanitized submission artifacts can now reconcile concrete tracker append read-back rows and Discord delivery read-back payloads against the expected submission record/message, surfacing `tracker_verified`, `notification_delivered`, per-channel match flags, and one final `consistent` verdict. Verified strict RED→GREEN with `python3 -m pytest tests/test_submission_artifacts.py::test_reconcile_submission_delivery_uses_tracker_read_back_and_notification_delivery_results -v` (initially `AttributeError: module 'submission_artifacts' has no attribute 'reconcile_submission_delivery'`, then passed), re-ran the focused suite (`python3 -m pytest tests/test_submission_artifacts.py -q` → `2 passed in 0.01s`), and ran the full suite (`python3 -m pytest tests -q` → `108 passed in 0.14s`). Next task: production-shaped idempotent dry run proving no duplicate queueing, no unsupported submissions, and no external side effects.
- 2026-08-24 23:27 PDT — Completed task 10 from the new backlog: extended `production_run.py` so the production-safe dry-run command now replays the orchestrator against the same persisted workspace to emit a machine-readable `verification` proof covering idempotent queueing, supported-only queue contents, `submission_enabled: false`, and blocked external side effects. Verified strict RED→GREEN with `python3 -m pytest tests/test_production_run.py::test_main_proves_idempotent_dry_run_and_keeps_unsupported_roles_out_of_queue -v` (initially failed on `KeyError: 'verification'`, then passed), re-ran the focused suite (`python3 -m pytest tests/test_production_run.py -q` → `2 passed in 0.02s`), and ran the full suite (`python3 -m pytest tests -q` → `109 passed in 0.13s`). Next task: backlog complete; queue a new prioritized sprint before further build work.
- 2026-08-25 00:33 PDT — Progress on post-backlog task 1: added `greenhouse_handler.py` plus `tests/test_greenhouse_handler.py` as the first executable Greenhouse safety-contract slice, covering application-surface inventory, company/role/location extraction, resume filename read-back verification, CAPTCHA/manual-gate detection plumbing, confirmation-evidence reuse, and a machine-readable CLI shape. Verified strict RED→GREEN with `python -m pytest tests/test_greenhouse_handler.py::test_inspect_application_fixture_inventories_fields_and_verifies_uploaded_resume -v` (initially `ModuleNotFoundError: No module named 'greenhouse_handler'`, then label parsing failed on `Work authorizationSelectYesNo`, then passed), and re-ran the full suite (`python -m pytest tests -q` → `110 passed in 0.18s`). Next slice: add Greenhouse confirmation/manual-gate fixtures and then lift Greenhouse/Lever/Oracle behind one shared ATS handler contract.
- 2026-08-25 00:34 PDT — Completed post-backlog task 2 directly at Kevin's request: added `workday_handler.py`, `tests/test_workday_handler.py`, and safe Workday fixtures covering application wizard/field inventory, tenant/listing entrypoint metadata, exact resume filename read-back, parsed-resume mismatch detection against source values, Save as Draft awareness, simultaneous CAPTCHA/email-verification/assessment gates, fail-closed CLI exit behavior, verified confirmation evidence, Workday reference-ID extraction, and explicit non-submitting start actions (`autofill_with_resume`, `apply_manually`, `use_last_application`). Integrated `ats_preflight.py` with the executable Workday handler safety contract. Verified strict RED→GREEN slices for missing module, parse issues, multi-gate reporting, confirmation reference, fail-closed CLI, preflight integration, and start-page action dispatch. A real non-submitting Medtronic Workday test verified listing `R73630`, the `/apply` start page, correct role/tenant extraction, and all three action URLs without opening or submitting an application. Full suite passes. Next task: shared ATS handler registry and non-submitting `prepare-job` dispatch.
- 2026-08-25 00:46 PDT — Completed post-backlog task 3: added `ats_registry.py`, `prepare_job.py`, and `tests/test_prepare_job.py` so one non-submitting `prepare-job` command now resolves Greenhouse, Workday, Lever, and Oracle handlers through a shared registry, preserves handler payloads under a common `platform`/`page_url`/`submission_enabled: false` envelope, and fails closed with machine-readable JSON for unsupported ATS URLs. Verified strict RED→GREEN with `python -m pytest tests/test_prepare_job.py::test_prepare_saved_html_dispatches_to_supported_ats_handlers -v` (initially failed on missing `page_url`), then `python -m pytest tests/test_prepare_job.py::test_main_fails_closed_with_machine_readable_error_for_unsupported_ats -v` (initially raised raw `ValueError`), re-ran the focused suite (`python -m pytest tests/test_prepare_job.py -q` → `2 passed in 0.01s`), and ran the full suite (`python -m pytest tests -q` → `120 passed in 0.14s`). Next task: integrate prepared ATS plans with the leased queue worker and a durable execution journal.
- 2026-08-25 01:01 PDT — Progress on post-backlog task 4: added `execution_journal.py`, `queue_worker.py`, and `tests/test_queue_worker.py` as the first durable queue-worker recovery slice so a leased attempt can resume from an already-written prepared-plan artifact instead of reparsing the form after a crash. Verified strict RED→GREEN with `python -m pytest tests/test_queue_worker.py::test_resume_or_prepare_leased_job_recovers_after_plan_was_already_written -v` (initially `ModuleNotFoundError: No module named 'execution_journal'`, then passed), and re-ran the full suite (`python -m pytest tests -q` → `122 passed in 0.15s`). Next slice: lease the next discovered job directly from the worker, journal the fresh prepare path, and persist deterministic plan artifacts without rework on retries.
- 2026-08-25 02:20 PDT — Progress on post-backlog task 4: extended `queue_worker.py` so the worker can now lease the next discovered job itself, journal a `lease_claimed` event before preparation, and persist deterministic attempt-scoped plan artifacts (`job-<id>-attempt-<n>.json`) on both first prepare and retry attempts. Verified strict RED→GREEN with `python -m pytest tests/test_queue_worker.py::test_prepare_next_job_leases_discovered_job_writes_plan_and_finishes_prepared -v` (initially `AttributeError: module 'queue_worker' has no attribute 'prepare_next_job'`, then passed) and `python -m pytest tests/test_queue_worker.py::test_prepare_next_job_retry_uses_attempt_specific_plan_path -v` (passed after the new worker entrypoint landed), re-ran the focused suite (`python -m pytest tests/test_queue_worker.py -q` → `3 passed in 0.02s`), and ran the full suite (`python -m pytest tests -q` → `124 passed in 0.16s`). Next slice: add a queue-worker CLI that leases once, writes the plan artifact path/result as machine-readable JSON, and keeps recovery idempotent across repeated invocations.
- 2026-08-25 02:28 PDT — Progress on post-backlog task 4: added a `queue_worker.py` CLI that leases at most one discovered job, emits machine-readable `prepared`/`no_job_available` results, writes the attempt-scoped plan artifact via the existing worker path, and stays idempotent on repeated invocations after the first prepare completes. Verified strict RED→GREEN with `python -m pytest tests/test_queue_worker.py::test_main_leases_once_writes_machine_readable_result_and_stays_idempotent -v` (initially `AttributeError: module 'queue_worker' has no attribute 'main'`, then passed), re-ran the focused suite (`python -m pytest tests/test_queue_worker.py -q` → `4 passed in 0.02s`), and ran the full suite (`python -m pytest tests -q` → `125 passed in 0.21s`). Next slice: fail closed in the CLI when prepare-time ATS parsing raises an unsupported/invalid preparation error, and journal the recovery-ready lease state without masking the blocker.
- 2026-08-25 02:40 PDT — Progress on post-backlog task 4: tightened `queue_worker.py` so the CLI now fails closed with machine-readable `prepare_blocked` output when `prepare_job.prepare_saved_html(...)` raises a preparation `ValueError`, requeues the leased job back to `discovered` with `last_error` preserved for retry/recovery, writes `prepare_blocked` + `lease_finished` journal entries, and avoids emitting any bogus plan artifact. Verified strict RED→GREEN with `python -m pytest tests/test_queue_worker.py::test_main_fails_closed_and_requeues_job_when_prepare_is_blocked -v` (initially leaked raw `ValueError: Unsupported ATS for URL: https://example.com/custom/apply`, then passed), re-ran the focused suite (`python -m pytest tests/test_queue_worker.py -q` → `5 passed in 0.03s`), and ran the full suite (`python -m pytest tests -q` → `126 passed in 0.17s`). Next slice: distinguish retryable preparation blockers from terminal ones so unsupported ATS or corrupted fixtures can fail closed without entering infinite rediscovery loops.
- 2026-08-25 02:50 PDT — Completed post-backlog task 4: refined `queue_worker.py` preparation-blocker handling so transient errors requeue safely with `retryable: true`, while unsupported ATS URLs and corrupt HTML fixtures are terminally failed with `retryable: false`; all paths retain the error and durable `prepare_blocked`/`lease_finished` journal evidence. Verified strict RED→GREEN with `python -m pytest tests/test_queue_worker.py::test_main_terminally_fails_unsupported_ats_without_requeueing -v` (initially failed with `KeyError: 'retryable'`, then passed) and `python -m pytest tests/test_queue_worker.py::test_main_terminally_fails_corrupted_preparation_fixture -v` (initially asserted `True is False`, then passed). Re-ran the full suite: `python -m pytest tests -q` → `128 passed in 0.15s`. Next task: versioned source registry for approved Greenhouse/Lever boards and deterministic registry-driven collection.
- 2026-08-25 02:56 PDT — Completed post-backlog task 5: added `source_registry.py` and registry-driven `sources.py --registry` collection. The versioned allowlist accepts only approved Greenhouse/Lever entries, filters disabled boards, deterministically sorts platform/token entries, and refuses ambiguous mixing with ad-hoc source flags. Verified strict RED→GREEN with `python -m pytest tests/test_source_registry.py::test_load_registry_returns_sorted_approved_greenhouse_and_lever_boards -v` (initially `ModuleNotFoundError: No module named 'source_registry'`, then passed) and `python -m pytest tests/test_source_registry.py::test_sources_main_collects_only_approved_registry_entries_in_deterministic_order -v` (initially rejected unrecognized `--registry`, then passed). Re-ran the focused suite (`python -m pytest tests/test_source_registry.py -v` → `2 passed`) and full suite (`python -m pytest tests -q` → `130 passed in 0.17s`). Next task: Kevin Bible Google Doc synchronization into a local ignored cache with source timestamps, resume-precedence conflict reporting, and no raw PII in logs.
- 2026-08-25 03:00 PDT — Progress on post-backlog task 6: added `kevin_bible_sync.py` plus `tests/test_kevin_bible_sync.py` as the first sync slice. It reads through the approved read-only Google Docs helper, persists the private ignored cache with document revision/modified/fetched timestamps, identifies profile-wins answer conflicts without exposing raw answer values, and logs only document IDs/counts. Verified strict RED→GREEN with `python -m pytest tests/test_kevin_bible_sync.py::test_sync_caches_source_timestamp_and_reports_profile_precedence_without_pii_in_logs -v` (initially `ModuleNotFoundError: No module named 'kevin_bible_sync'`, then passed) and full suite (`python -m pytest tests -q` → `131 passed in 0.17s`). Next slice: add a safe CLI/default cache location and turn Google Docs structured content into answer entries while retaining fail-closed source validation.
- 2026-08-25 03:08 PDT — Completed post-backlog task 6: extended `kevin_bible_sync.py` with an explicit bounded `[answers]` Google Docs text format, private ignored `runtime/kevin-bible-cache.json` default CLI cache, sanitized machine-readable CLI summary, and read-only Drive metadata enrichment so the cached source carries a verified modified timestamp even though the Docs helper emits text-only payloads. The fetch path fails closed if either Docs/Drive document identity or timestamp is invalid; logs/CLI summaries retain only IDs, counts, and conflict metadata. Verified strict RED→GREEN with `python -m pytest tests/test_kevin_bible_sync.py::test_sync_parses_structured_google_doc_body_into_answer_entries -v` (initially cached `answers == []`), `python -m pytest tests/test_kevin_bible_sync.py::test_main_uses_private_default_cache_and_emits_sanitized_summary -v` (initially missing `DEFAULT_CACHE_PATH`/`main`), and `python -m pytest tests/test_kevin_bible_sync.py::test_fetch_google_document_enriches_docs_text_with_drive_source_timestamp -v` (initially `KeyError: 'modifiedTime'`), then full suite (`python -m pytest tests -q` → `134 passed in 0.18s`). Next task: answer-coverage preflight matrix that reports known, company-specific, optional-skip, and human-required questions before opening a form.
- 2026-08-25 03:20 PDT — Completed post-backlog task 7: added `answer_coverage.py` and `tests/test_answer_coverage.py` for a non-submitting, answer-value-free preflight matrix. It classifies questions as profile/generic known, company-specific Kevin Bible, optional-skip, or human-required with a fail-closed reason. Verified strict RED→GREEN with `python -m pytest tests/test_answer_coverage.py::test_coverage_matrix_reports_profile_answer_as_known -v` (initially `ModuleNotFoundError: No module named 'answer_coverage'`), `python -m pytest tests/test_answer_coverage.py::test_coverage_matrix_separates_company_specific_doc_answer -v` (initially unexpected `company` keyword), and `python -m pytest tests/test_answer_coverage.py::test_coverage_matrix_skips_optional_questions_and_escalates_unknown_required_ones -v` (initially empty `optional_skip`). Re-ran focused tests (`3 passed`) and the full suite (`python -m pytest tests -q` → `137 passed in 0.21s`). Next task: Discord job-ID approval commands with authorization, expiry, replay protection, and audit events.
- 2026-08-25 03:09 PDT — Progress on post-backlog task 8: extended queue-bound Discord control handling with explicit actor allowlist enforcement and structured `discord_queue_control_applied` audit evidence for authorized state transitions. Verified strict RED→GREEN with `python -m pytest tests/test_discord_controls.py::test_handle_control_authorizes_actor_and_writes_audit_event -v` (initially `TypeError: handle_control() got an unexpected keyword argument 'actor_id'`, then `1 passed`) and full suite (`python -m pytest tests -q` → `138 passed in 0.17s`). Next slice: reject unauthorized actors without changing queue state and record a redacted denial audit event; then add expiring, single-use command tokens.
- 2026-08-25 03:25 PDT — Progress on post-backlog task 8: denial of an unauthorized Discord queue-control actor now leaves the job state unchanged and writes a `discord_queue_control_denied` audit record with a stable `unauthorized_actor` reason. Verified strict RED→GREEN with `python -m pytest tests/test_discord_controls.py::test_handle_control_denies_unauthorized_actor_without_transition_and_audits_denial -v` (initially failed because no audit file existed), then full suite (`python -m pytest tests -q` → `139 passed in 0.21s`). Next slice: add expiring, single-use command tokens with replay protection.
- 2026-08-25 03:36 PDT — Completed post-backlog task 8: added durable actor- and control-bound Discord command tokens in the local queue. Tokens expire deterministically, are atomically consumed once, and replay/invalid/expired-token denials leave queue state unchanged while emitting redacted denial audit evidence; token values never enter audit payloads. Verified strict RED→GREEN with `python -m pytest tests/test_discord_controls.py::test_handle_control_accepts_single_use_token_once_and_rejects_replay -v` (initially `AttributeError: module 'discord_controls' has no attribute 'issue_control_token'`, then passed); an expiration regression was attempted and passed immediately because expiry protection is supplied by the same new implementation, so it was removed rather than represented as a separate RED→GREEN slice. Full suite: `python -m pytest tests -q` → `140 passed in 0.20s`. Next task: bidirectional queue/Google-Sheet state reconciliation that detects drift without overwriting newer verified states.
- 2026-08-25 03:40 PDT — Progress on post-backlog task 9: added read-only `queue_sheet_reconciliation.reconcile(...)` as the first drift-detection slice. A verified `Submitted - Pending Response` tracker row matched by normalized official URL is now reported when local queue state lags, with an explicit no-mutations contract so the newer verified Sheet state is never overwritten. Verified strict RED→GREEN with `python -m pytest tests/test_tracker.py::test_reconcile_reports_verified_sheet_submission_drift_without_mutating_queue -v` (initially `ModuleNotFoundError: No module named 'queue_sheet_reconciliation'`, then passed) and full suite (`python -m pytest tests -q` → `141 passed in 0.18s`). Next slice: report newer terminal local queue states against stale Sheet rows, still read-only.
- 2026-08-25 03:26 PDT — Progress on post-backlog task 9: extended the read-only reconciler to report terminal local queue states (`failed`/`applied`) matched to stale `Discovered` tracker rows, still with an explicit empty mutations contract. Verified strict RED→GREEN with `python -m pytest tests/test_tracker.py::test_reconcile_reports_terminal_queue_failure_against_stale_sheet_row -v` (initially failed because the reconciler returned no drift, then passed) and full suite (`python -m pytest tests -q` → `142 passed in 0.20s`). Next slice: report terminal tracker rows against nonterminal local queue states without overwriting either system.
- 2026-08-25 03:49 PDT — Progress on post-backlog task 9: extended the read-only reconciler to report a terminal `Rejected` tracker row against a nonterminal local queue job as `sheet_state_is_newer_terminal`, preserving the explicit no-mutations contract. Verified strict RED→GREEN with `python -m pytest tests/test_tracker.py::test_reconcile_reports_terminal_sheet_rejection_against_nonterminal_queue_job -v` (initially returned no drift, then `1 passed`) and full suite (`python -m pytest tests -q` → `143 passed in 0.19s`). Next slice: cover additional terminal tracker statuses without overwriting either system.
- 2026-08-25 03:55 PDT — Completed post-backlog task 9: expanded terminal tracker-state drift detection to include `Withdrawn`; a matched nonterminal queue job now reports `sheet_state_is_newer_terminal` while both sources remain read-only. Verified strict RED→GREEN with `python -m pytest tests/test_tracker.py::test_reconcile_reports_terminal_sheet_withdrawal_against_nonterminal_queue_job -v` (initially `AssertionError: [] == [...]`, then `1 passed`), focused tracker suite (`10 passed in 0.03s`), and full suite (`python -m pytest tests -q` → `144 passed in 0.21s`). Next task: stale/closed posting detection and terminal queue closure before form preparation.
- 2026-08-25 04:02 PDT — Progress on post-backlog task 10: added fail-closed pre-preparation closure detection to the leased worker. A captured application surface that explicitly says `This job is no longer available.` now terminally closes the queue job with retained evidence, journals the blocked lease, and returns non-submitting machine-readable `posting_closed`; it is never requeued. Verified strict RED→GREEN with `python -m pytest tests/test_queue_worker.py::test_main_terminally_closes_withdrawn_posting_before_preparation -v` (initially prepared the withdrawn HTML and returned exit `0`, then `1 passed`) and full suite (`python -m pytest tests -q` → `145 passed in 0.19s`). Next slice: cover another verified closure phrase or detect ATS-native closure metadata before preparation.
- 2026-08-25 03:47 PDT — Completed post-backlog task 10: extended fail-closed pre-preparation closure detection for the explicit rendered phrase `This job is no longer accepting applications.` The leased worker terminally records the closure as `posting_closed`, preserves the exact evidence in `last_error`, and never requeues the job. Verified strict RED→GREEN with `python -m pytest tests/test_queue_worker.py::test_main_terminally_closes_posting_that_is_no_longer_accepting_applications -v` (initially failed because the worker returned exit `0`, then `1 passed`), focused queue-worker suite (`9 passed in 0.04s`), and full suite (`python -m pytest tests -q` → `146 passed in 0.20s`). Next task: per-ATS rate limits, retry budgets, and circuit breakers so a failing tenant cannot stall the entire worker.
- 2026-08-25 03:52 PDT — Progress on post-backlog task 11: added durable per-ATS circuit-breaker cooldowns and queue leasing exclusions, so an open Greenhouse circuit leaves its job discovered while the worker leases and prepares an eligible Lever job instead. Verified strict RED→GREEN with `python -m pytest tests/test_queue_worker.py::test_prepare_next_job_skips_platform_with_open_circuit_and_leases_other_ats -v` (initially `AttributeError: module 'queue_worker' has no attribute 'ATSCircuitBreaker'`, then `1 passed`) and full suite (`python -m pytest tests -q` → `147 passed in 0.20s`). Next slice: record retryable worker failures into the matching ATS circuit and add bounded retry budgets.
- 2026-08-25 03:57 PDT — Progress on post-backlog task 11: the non-submitting queue-worker CLI now accepts an optional durable circuit database and, on a retryable preparation failure, records the leased job’s ATS in its configured cooldown before safe requeue. Verified strict RED→GREEN with `python -m pytest tests/test_queue_worker.py::test_main_records_retryable_preparation_failure_in_platform_circuit -v` (initially failed because `--circuit-db` and `--circuit-cooldown-seconds` were unrecognized, then `1 passed`) and full suite (`python -m pytest tests -q` → `148 passed in 0.20s`). Next slice: add bounded per-ATS retry budgets so repeated failures terminally contain only the offending platform.
- 2026-08-25 04:04 PDT — Completed post-backlog task 11: added durable per-ATS retry failure counts to the circuit database plus `queue_worker.py --ats-retry-budget` (default 3). A retryable preparation failure that reaches the configured platform budget now terminally fails only that leased job, retains its error, reports `retry_budget_exhausted`, and leaves other ATS jobs discoverable; lower-count failures retain the existing safe requeue/cooldown path. Verified strict RED→GREEN with `python -m pytest tests/test_queue_worker.py::test_main_terminally_contains_platform_when_retry_budget_is_exhausted -v` (initially failed on unrecognized `--ats-retry-budget`, then `1 passed`), focused queue-worker suite (`12 passed in 0.05s`), and full suite (`python -m pytest tests -q` → `149 passed in 0.21s`). Next task: machine-readable operational health report covering sources, browser, OAuth, queue leases, tracker drift, and notification delivery.
- 2026-08-25 04:08 PDT — Progress on post-backlog task 12: added `operational_health.py` plus `tests/test_operational_health.py` as the first read-only report slice. It composes source status, browser probe evidence, OAuth availability, active lease counts, tracker drift counts, and notification delivery evidence into a machine-readable overall `ready`/`degraded` result without mutating queue, tracker, or notification state. Verified strict RED→GREEN with `python -m pytest tests/test_operational_health.py::test_build_report_summarizes_all_required_operational_surfaces -v` (initially `ModuleNotFoundError: No module named 'operational_health'`, then `1 passed`) and full suite (`python -m pytest tests -q` → `150 passed in 0.21s`). Next slice: add concrete read-only source/browser/OAuth/queue/tracker/notification probes and CLI wiring.
- 2026-08-25 04:15 PDT — Completed post-backlog task 12: wired `operational_health.py` into a read-only CLI. It now loads persisted source and optional notification evidence, probes CDP browser readiness, checks OAuth-token availability, opens the local queue for lease counts, and reconciles either a supplied tracker snapshot or a read-only Sheets API fetch; it emits one machine-readable report and returns nonzero when drift degrades it. Verified strict RED→GREEN with `python -m pytest tests/test_operational_health.py::test_main_runs_read_only_probes_and_emits_machine_readable_report -v` (initially `AttributeError: module 'operational_health' has no attribute 'browser_health'`, then `1 passed`) and full suite (`python -m pytest tests -q` → `151 passed in 0.25s`). Next task: resume selection and filename/content preflight so the chosen resume is confirmed before each preparation.
- 2026-08-25 04:21 PDT — Progress on post-backlog task 13: added `resume_preflight.py` and `tests/test_resume_preflight.py` as the first fail-closed resume-selection slice. It verifies the profile-selected path is allowed, exists, has the required application filename, and contains PDF content, emitting only safe filename/type/size/hash evidence. Verified strict RED with `python -m pytest tests/test_resume_preflight.py::test_preflight_accepts_profile_selected_pdf_with_required_filename -v` (initially `ModuleNotFoundError: No module named 'resume_preflight'`), then GREEN (`2 passed`) and full suite (`python -m pytest tests -q` → `153 passed in 0.34s`). Next slice: wire verified resume evidence into every non-submitting preparation entrypoint.
- 2026-08-25 04:28 PDT — Completed post-backlog task 13: wired profile-derived, fail-closed resume preflight into both non-submitting preparation CLIs (`prepare_job.py --profile` and `queue_worker.py --profile`). Each derives the expected upload basename from the verified profile resume and persists safe preflight evidence plus `resume_verified: true` in its preparation payload; a failed preflight blocks before a queue lease. Verified strict RED→GREEN with `python -m pytest tests/test_prepare_job.py::test_main_uses_profile_resume_preflight_and_embeds_safe_evidence -v` (initially argparse rejected `--profile`, then `1 passed`) and `python -m pytest tests/test_queue_worker.py::test_main_uses_profile_resume_preflight_in_persisted_plan -v` (initially argparse rejected `--profile`, then `1 passed`), focused suites (`3 passed`, `13 passed`), and full suite: `python -m pytest tests -q` → `155 passed in 0.22s`. Next task: non-submitting end-to-end fixtures for every handler through source → queue → answer engine → ATS plan → evidence artifact.
- 2026-08-25 04:35 PDT — Progress on post-backlog task 14: added `fixture_e2e.py` and a non-submitting Greenhouse vertical E2E fixture that carries a source candidate through idempotent queueing, profile-backed answer coverage, a leased durable ATS preparation plan, and a sanitized confirmation-evidence artifact. It preserves `submission_enabled: false`; no tracker or notification mutation occurs. Verified strict RED→GREEN with `python -m pytest tests/test_fixture_e2e.py::test_run_greenhouse_fixture_flow_prepares_queue_answer_coverage_plan_and_evidence -v` (initially `ModuleNotFoundError: No module named 'fixture_e2e'`, then `1 passed`), then full suite: `python -m pytest tests -q` → `156 passed in 0.23s`. Next slice: apply the same E2E contract to Workday.
- 2026-08-25 04:43 PDT — Progress on post-backlog task 14: added an explicit `run_workday_fixture_flow` non-submitting vertical E2E contract. It proves a Workday source candidate is queued, profile-backed answer coverage is generated, a wizard preparation plan verifies the uploaded resume, and sanitized confirmation evidence reconciles without tracker/notification mutation. The first RED run failed as expected because the Workday E2E entrypoint was missing; the initial GREEN exposed a real confirmation-validation gap for the ordinary phrase `Your application has been received`, which is now accepted alongside existing success phrases. Verified targeted Workday E2E (`1 passed`), all E2E tests (`2 passed`), and full suite: `python -m pytest tests -q` → `157 passed in 0.23s`. Next slice: apply the same E2E contract to Lever.
- 2026-08-25 04:48 PDT — Progress on post-backlog task 14: added `run_lever_fixture_flow`, completing the same non-submitting source → queue → answer coverage → ATS plan → sanitized evidence contract for Lever fixtures. Strict RED was observed with `python -m pytest tests/test_fixture_e2e.py::test_run_lever_fixture_flow_prepares_non_submitting_plan_and_evidence -v` failing on missing `run_lever_fixture_flow`; GREEN passed the targeted regression (`1 passed`) and full suite: `python -m pytest tests -q` → `158 passed in 0.26s`. Next slice: apply the same E2E contract to Oracle.
- 2026-08-25 04:45 PDT — Completed post-backlog task 14: added `run_oracle_fixture_flow`, completing the non-submitting source → queue → answer coverage → ATS preparation plan → sanitized confirmation-evidence artifact contract for all supported Greenhouse, Workday, Lever, and Oracle handlers. Strict RED was observed with `python -m pytest tests/test_fixture_e2e.py::test_run_oracle_fixture_flow_prepares_non_submitting_plan_and_evidence -v` failing on the missing Oracle E2E entrypoint; after adding the minimal shared-flow adapter, GREEN passed (`1 passed`) and the full suite passed: `python -m pytest tests -q` → `159 passed in 0.24s`. No application, tracker, or notification mutations occur (`submission_enabled: false`). Next task: final production-readiness audit and idempotent dry run; document remaining human-only gates without weakening safeguards.
- 2026-08-25 04:55 PDT — Completed post-backlog task 15: added `production_readiness.py`, a read-only audit CLI requiring persisted production-safe dry-run verification plus non-submitting Greenhouse/Workday/Lever/Oracle fixture-flow evidence. It reports `ready_for_human_gated_production` only when idempotent queueing, unsupported-role exclusion, disabled submission/external side effects, and all four exact handler plans are verified; it documents CAPTCHA, email/identity verification, assessments, unknown required questions, and explicit submission authorization as remaining human-only gates. Strict RED→GREEN: `python -m pytest tests/test_production_readiness.py::test_audit_requires_all_supported_fixture_flows_and_reports_human_only_gates -v` initially failed with `ModuleNotFoundError`, then passed; CLI evidence test initially failed with missing `main`, then passed. Re-verified idempotent production-safe dry run (`python -m pytest tests/test_production_run.py::test_main_proves_idempotent_dry_run_and_keeps_unsupported_roles_out_of_queue -v` → `1 passed`) and full suite: `python -m pytest tests -q` → `161 passed in 0.32s`. Post-backlog queue complete.
- 2026-08-25 06:20 PDT — Completed post-backlog task 1 safety-contract gap: Greenhouse inspection now fail-closes on a rendered email-verification gate, matching the non-submitting manual-gate policy used by the supported ATS handlers. Strict RED→GREEN: `python -m pytest tests/test_greenhouse_handler.py::test_inspect_html_fails_closed_on_greenhouse_email_verification_gate -v` initially failed because `manual_gate` was `None`, then passed; focused suite `2 passed` and full suite `python -m pytest tests -q` → `162 passed in 0.29s`. No external side effects.
- 2026-08-25 06:42 PDT — Completed post-backlog task 1 CLI safety-contract slice: Greenhouse's machine-readable CLI now returns exit `2` and `safe_to_prepare: false` for an application surface blocked by a manual gate, rather than reporting ordinary CLI success. Strict RED→GREEN: `python -m pytest tests/test_greenhouse_handler.py::test_main_fails_closed_for_greenhouse_manual_gate_and_emits_json -v` initially failed on `assert 0 == 2`, then passed; focused suite `3 passed` and full suite `python -m pytest tests -q` → `163 passed in 0.25s`. No external side effects.
- 2026-08-25 06:46 PDT — Progress on post-backlog task 1 safety-contract slice: Greenhouse inspection now fail-closes when the rendered surface requires an assessment, returning a structured `assessment` manual gate and `safe_to_prepare: false`. Strict RED→GREEN: `python -m pytest tests/test_greenhouse_handler.py::test_inspect_html_fails_closed_on_greenhouse_assessment_gate -v` initially failed on `assert True is False`, then passed; full suite `python -m pytest tests -q` → `164 passed in 0.26s`. No external side effects. Next slice: detect identity-verification gates before preparation.
- 2026-08-25 06:51 PDT — Completed post-backlog task 1 safety-contract slice: Greenhouse inspection now fail-closes on rendered identity-verification requirements, returning a structured `identity_verification` manual gate and `safe_to_prepare: false`. Strict RED→GREEN: `python -m pytest tests/test_greenhouse_handler.py::test_inspect_html_fails_closed_on_greenhouse_identity_verification_gate -v` initially failed on `assert True is False`, then passed. Full suite: `python -m pytest tests -q` → `165 passed in 0.24s`. No external side effects. Next slice: detect equivalent identity proofing wording before preparation.
- 2026-08-25 06:56 PDT — Completed post-backlog task 1 safety-contract slice: Greenhouse inspection now also fail-closes when a rendered page asks the candidate to `verify your identity`, classifying it as the existing `identity_verification` manual gate. Strict RED→GREEN: `python -m pytest tests/test_greenhouse_handler.py::test_inspect_html_fails_closed_when_greenhouse_asks_to_verify_identity -v` initially failed on `assert True is False`, then passed. Full suite: `python -m pytest tests -q` → `166 passed in 0.25s`. No external side effects. Next slice: detect equivalent assessment wording before preparation.
- 2026-08-25 07:00 PDT — Completed post-backlog task 1 safety-contract slice: Greenhouse inspection now treats a rendered `skills test` requirement as an `assessment` manual gate and fails closed before preparation. Strict RED→GREEN: `python -m pytest tests/test_greenhouse_handler.py::test_inspect_html_fails_closed_when_greenhouse_requires_a_skills_test -v` initially failed on `assert True is False`, then passed. Full suite: `python -m pytest tests -q` → `167 passed in 0.28s`. No external side effects. Next slice: detect equivalent email-verification wording before preparation.
- 2026-08-25 07:05 PDT — Completed post-backlog task 1 safety-contract slice: Greenhouse inspection now treats a rendered `Confirm your email address` requirement as the existing `email_verification` manual gate and fails closed before preparation. Strict RED→GREEN: `python -m pytest tests/test_greenhouse_handler.py::test_inspect_html_fails_closed_when_greenhouse_asks_to_confirm_email_address -v` initially failed on `assert True is False`, then passed. Full suite: `python -m pytest tests -q` → `168 passed in 0.24s`. No external side effects. Next slice: detect an additional equivalent email-verification wording before preparation.
- 2026-08-25 07:10 PDT — Completed post-backlog task 1 safety-contract slice: Greenhouse inspection now treats rendered `Verify email address` wording as the existing `email_verification` manual gate and fails closed before preparation. Strict RED→GREEN: `python -m pytest tests/test_greenhouse_handler.py::test_inspect_html_fails_closed_when_greenhouse_asks_to_verify_email_address -v` initially failed on `assert True is False`, then passed. Full suite: `python -m pytest tests -q` → `169 passed in 0.41s`. No external side effects. Next slice: detect an additional equivalent assessment wording before preparation.
- 2026-08-25 07:15 PDT — Completed post-backlog task 1 safety-contract slice: Greenhouse inspection now treats a rendered `online evaluation` requirement as an `assessment` manual gate and fails closed before any preparation. Strict RED→GREEN: `python -m pytest tests/test_greenhouse_handler.py::test_inspect_html_fails_closed_when_greenhouse_requires_an_online_evaluation -v` initially failed on `assert True is False`, then passed. Full suite: `python -m pytest tests -q` → `170 passed in 0.23s`. No external side effects. Next slice: detect another equivalent manual assessment wording before preparation.
- 2026-08-25 07:24 PDT — Completed post-backlog task 2 safety-contract slice: Workday inspection now fail-closes on rendered identity-verification requirements, returning a structured `identity_verification` manual gate before any non-submitting preparation. Strict RED→GREEN: `python -m pytest tests/test_workday_handler.py::test_inspect_html_fails_closed_on_identity_verification_requirement -v` initially failed with an empty `manual_gates` list, then passed. Focused Workday suite: `8 passed`; full suite: `python -m pytest tests -q` → `171 passed in 0.24s`. No external side effects. Next task: ATS registry and non-submitting `prepare-job` dispatch.
- 2026-08-25 07:28 PDT — Completed post-backlog task 1 safety-contract slice: Greenhouse inspection now treats a rendered `coding challenge` requirement as an `assessment` manual gate and fails closed before preparation. Strict RED→GREEN: `python -m pytest tests/test_greenhouse_handler.py::test_inspect_html_fails_closed_when_greenhouse_requires_a_coding_challenge -v` initially failed because `safe_to_prepare` was `True`, then passed. Full suite: `python -m pytest tests -q` → `172 passed in 0.34s`. No external side effects. Next task: ATS registry and non-submitting `prepare-job` dispatch.
- 2026-08-25 07:38 PDT — Completed post-backlog task 1 handler-contract slice: Greenhouse field inventory now binds standard separate `<label for="…">` controls as well as nested controls, matching the supported handler field-inventory contract without mutating any ATS state. Strict RED→GREEN: `python -m pytest tests/test_greenhouse_handler.py::test_inspect_html_binds_a_for_label_to_its_separate_greenhouse_control -v` initially failed with `fields == []`, then passed. Full suite: `python -m pytest tests -q` → `173 passed in 0.26s`. No external side effects. Next task: ATS registry and non-submitting `prepare-job` dispatch.
- 2026-08-25 07:51 PDT — Completed post-backlog task 3 CLI safety-contract slice: the shared non-submitting `prepare_job.py` dispatcher now returns exit `2` when any supported ATS handler reports `safe_to_prepare: false`, preserving the handler payload and disabled-submission invariant rather than presenting a manual gate as successful preparation. Strict RED→GREEN: `python -m pytest tests/test_prepare_job.py::test_main_fails_closed_for_supported_ats_manual_gate -v` initially failed on `assert 0 == 2`, then passed. Focused suite: `4 passed`; full suite: `python -m pytest tests -q` → `174 passed in 0.25s`. No external side effects. Next task: handler-to-leased-worker execution journal integration.
- 2026-08-25 08:00 PDT — Completed handler-to-worker circuit integration slice: the `queue_worker.py` CLI now consults its configured durable per-ATS circuit breaker before leasing, so an open Greenhouse circuit leaves that job untouched and prepares an eligible Lever job instead. Strict RED→GREEN: `python -m pytest tests/test_queue_worker.py::test_main_skips_open_circuit_platform_and_prepares_another_ats_job -v` initially failed because the CLI leased `Greenhouse`, then passed after propagating the circuit exclusion into `lease_next`. Full suite: `python -m pytest tests -q` → `175 passed in 0.27s`. No external side effects. Next slice: ensure durable journal recovery validates its recorded plan artifact before finishing a recovered lease.
- 2026-08-25 08:06 PDT — Completed handler-to-worker recovery safety slice: a journaled `prepared_plan_written` entry now must reference an existing plan artifact before a recovered lease can transition to `prepared`; a missing artifact blocks with a stable error and leaves the lease/journal unchanged for recovery. Strict RED→GREEN: `python -m pytest tests/test_queue_worker.py::test_resume_or_prepare_leased_job_refuses_missing_recovered_plan_artifact -v` initially failed because the missing artifact was accepted and the lease was finished, then passed after file validation was added. Full suite: `python -m pytest tests -q` → `176 passed in 0.33s`. No external side effects. Next slice: validate malformed recovered plan content before finishing a recovered lease.
- 2026-08-25 08:10 PDT — Completed handler-to-worker recovery safety slice: a journaled recovered plan must now be a readable JSON object, not merely an existing file; malformed content blocks lease completion with the stable artifact error and preserves the leased state/journal for recovery. Strict RED→GREEN: `python -m pytest tests/test_queue_worker.py::test_resume_or_prepare_leased_job_refuses_malformed_recovered_plan_artifact -v` initially failed because malformed JSON was accepted and the lease was finished, then passed after JSON-object validation. Focused worker suite: `16 passed`; full suite: `python -m pytest tests -q` → `177 passed in 0.26s`. No external side effects. Next task: complete only the next explicitly prioritized backlog item (or revise `SPRINT.md` with a new one); do not invent micro-validation.
- 2026-08-25 08:26 PDT — Completed post-backlog task 1 handler-contract slice: Greenhouse now recognizes an explicit listing Apply entrypoint and classifies a no-field pre-form surface as `listing`, exposing the label while failing closed (`safe_to_prepare: false`) until the actual application form is loaded. Strict RED→GREEN: `python -m pytest tests/test_greenhouse_handler.py::test_inspect_html_classifies_greenhouse_listing_with_apply_entrypoint -v` initially failed with `application`, then passed; full suite: `python -m pytest tests -q` → `178 passed in 0.29s`. No external side effects. Next task: implement the ordered application-speed backlog below.

## Application-speed engineering queue — completed

Explicit priority from Kevin after a 40-minute first-time CGI/Njoyn application: reduce learned-ATS application time without weakening truthful answers, parser correction, Review verification, confirmation evidence, tracker read-back, or Discord read-back.

1. Add deterministic browser-action primitives with post-action verification contracts for replace-text, native select, radio/checkbox, CDP upload, scroll-and-click, and submit/confirm.
2. Build sanitized CGI/Njoyn fixtures covering listing, login/new profile, privacy, disclosures, disability, resume upload, parsed profile, referral, questionnaire, and confirmation pages from the verified live flow.
3. Implement an executable `njoyn_handler.py` that inventories each surface, maps controls, detects manual gates, verifies resume/parser state, and extracts exact confirmation evidence without submitting by default.
4. Register CGI/Njoyn in the shared ATS registry and `prepare-job` dispatcher while preserving `submission_enabled: false` and unsupported-surface fail-closed behavior.
5. Add a secure runtime credential adapter contract for account creation/sign-in that reads approved Keychain metadata without logging, serializing, or exposing secrets.
6. Add a deterministic page answer-map executor that fills a complete known page in one bounded browser operation and returns field-by-field read-back evidence.
7. Add retry/recovery policy primitives: one normal attempt, one inspected recovery, then a stable blocker; never loop blindly or duplicate submission.
8. Wire account/session reuse and learned tenant metadata into preparation plans so repeat applications skip rediscovery and account creation when authenticated state exists.
9. Add non-submitting vertical E2E fixtures for CGI/Njoyn through source → queue → answer coverage → plan → resume/parser repair evidence → confirmation artifact.
10. Add performance benchmarks and a production-readiness audit targeting learned ATS preparation in under 5 minutes and full verified submission in under 10 minutes, with safety invariants unchanged.

Implement strictly in order with RED → GREEN → REFACTOR. Each run completes at most one coherent slice, runs the focused test and full suite, commits verified work, appends evidence here, and returns `[SILENT]`. No real applications, external messages, email writes, tracker writes, credential mutations, or production submissions during build mode.

- 2026-08-25 13:30 PDT — Progress on application-speed task 1: added `browser_actions.py` with the first deterministic browser-action primitive, `replace_text`. It invokes the page seam then returns exact expected/actual post-action read-back evidence, so a visible keystroke alone cannot count as completion. Strict RED→GREEN: `python -m pytest tests/test_browser_actions.py::test_replace_text_returns_exact_post_action_read_back_evidence -v` initially failed with `ModuleNotFoundError: No module named 'browser_actions'`, then passed (`1 passed`). Full suite: `python -m pytest tests -q` → `179 passed in 0.40s`. No external side effects. Next exact slice: add the native-select primitive with selected-option read-back verification.
- 2026-08-25 13:36 PDT — Progress on application-speed task 1: added `browser_actions.native_select`, a deterministic native dropdown contract that calls the select seam and returns exact selected-option post-action read-back evidence. Strict RED→GREEN: `python -m pytest tests/test_browser_actions.py::test_native_select_returns_selected_option_read_back_evidence -v` initially failed with `AttributeError: module 'browser_actions' has no attribute 'native_select'`, then passed (`1 passed`). Updated `README.md`; full suite: `python -m pytest tests -q` → `180 passed in 0.34s`. No external side effects. Next exact slice: add the radio/checkbox primitive with checked-state read-back verification.
- 2026-08-25 13:43 PDT — Progress on application-speed task 1: added `browser_actions.set_checked`, a deterministic radio/checkbox contract that sets the intended state and returns exact checked-state post-action read-back evidence. Strict RED→GREEN: `python -m pytest tests/test_browser_actions.py::test_set_checked_returns_checked_state_read_back_evidence -v` initially failed with `AttributeError: module 'browser_actions' has no attribute 'set_checked'`, then passed (`1 passed`). Updated `README.md`; full suite: `python -m pytest tests -q` → `181 passed in 0.49s`. No external side effects. Next exact slice: add the CDP upload primitive with attached-file read-back verification.
- 2026-08-25 13:45 PDT — Progress on application-speed task 1: added `browser_actions.cdp_upload`, a deterministic CDP file-attachment contract that returns the expected basename and exact attached-filename read-back evidence. Strict RED→GREEN: `python -m pytest tests/test_browser_actions.py::test_cdp_upload_returns_attached_file_read_back_evidence -v` initially failed with `AttributeError: module 'browser_actions' has no attribute 'cdp_upload'`, then passed (`1 passed`). Updated `README.md`; full suite: `python -m pytest tests -q` → `182 passed in 0.34s`. No external side effects. Next exact slice: add the scroll-and-click primitive with post-click state read-back verification.
- 2026-08-25 13:48 PDT — Progress on application-speed task 1: added `browser_actions.scroll_and_click`, which scrolls to a control, clicks once, and returns exact expected/actual post-click state evidence so the click is not treated as complete without read-back. Strict RED→GREEN: `python -m pytest tests/test_browser_actions.py::test_scroll_and_click_returns_post_click_state_read_back_evidence -v` initially failed with `AttributeError: module 'browser_actions' has no attribute 'scroll_and_click'`, then passed (`1 passed`). Updated `README.md`; full suite: `python -m pytest tests -q` → `183 passed in 0.28s`. No external side effects. Next exact slice: add the submit/confirm primitive with explicit confirmation read-back verification.
- 2026-08-25 14:01 PDT — Completed application-speed task 1: added `browser_actions.submit_and_confirm`, a one-shot submit contract that returns exact expected/actual explicit confirmation read-back evidence and never treats a submit click alone as success. Strict RED→GREEN: `python -m pytest tests/test_browser_actions.py::test_submit_and_confirm_returns_explicit_confirmation_read_back_evidence -v` initially failed with `AttributeError: module 'browser_actions' has no attribute 'submit_and_confirm'`, then passed (`1 passed`). Updated `README.md`; full suite: `python -m pytest tests -q` → `184 passed in 0.27s`. No external side effects. Next task: build the ordered sanitized CGI/Njoyn fixtures (task 2).
- 2026-08-25 14:00 PDT — Completed application-speed task 2: added a sanitized CGI/Njoyn fixture set for listing, account creation/sign-in, privacy, disclosures, disability, resume upload, parsed-profile correction, referral, questionnaire, and confirmation surfaces. The contract test verifies every surface marker and rejects known personal contact/address identifiers. Strict RED→GREEN: `python -m pytest tests/test_njoyn_fixtures.py::test_sanitized_njoyn_fixture_set_covers_each_verified_application_surface -v` initially failed because `njoyn_listing.html` was absent, then passed (`1 passed`). Full suite: `python -m pytest tests -q` → `185 passed in 0.27s`. No external side effects. Next task: implement `njoyn_handler.py` (task 3).
- 2026-08-25 14:05 PDT — Progress on application-speed task 3: added the first executable, non-mutating `njoyn_handler.py` slice. It recognizes the sanitized Njoyn listing surface, inventories the role/company/location and real Apply entrypoint, and fails closed (`safe_to_prepare: false`) pending an application surface. Strict RED→GREEN: `python -m pytest tests/test_njoyn_handler.py::test_inspect_html_classifies_njoyn_listing_and_exposes_apply_entrypoint -v` initially failed with `ModuleNotFoundError: No module named 'njoyn_handler'`, then passed (`1 passed`). Full suite: `python -m pytest tests -q` → `186 passed in 0.29s`. No external side effects. Next exact slice: inventory account/login controls and fail closed on the account surface.
- 2026-08-25 14:12 PDT — Progress on application-speed task 3: `njoyn_handler.py` now inventories sanitized Njoyn account email/password controls, detects the real Create a profile entrypoint, and classifies sign-in/profile creation as the structured `account_sign_in` manual gate with `safe_to_prepare: false`. Strict RED→GREEN: `python -m pytest tests/test_njoyn_handler.py::test_inspect_html_inventories_njoyn_account_controls_and_fails_closed -v` initially failed because the account surface was misclassified as `application`, then passed (`1 passed`). Focused suite: `2 passed`; full suite: `python -m pytest tests -q` → `187 passed in 0.32s`. Updated README. No external side effects. Next exact slice: inventory the privacy surface and fail closed until its disclosure is explicitly handled.
- 2026-08-25 14:15 PDT — Progress on application-speed task 3: `njoyn_handler.py` now classifies a rendered Njoyn privacy notice, inventories its acknowledgement checkbox with an exact label read-back, and fail-closes it as a `privacy_notice` manual gate until the disclosure is explicitly handled. Strict RED→GREEN: `python -m pytest tests/test_njoyn_handler.py::test_inspect_html_inventories_njoyn_privacy_surface_and_fails_closed -v` initially failed because the surface was classified as `application`, then passed (`1 passed`). Focused Njoyn suite: `3 passed`; full suite: `python -m pytest tests -q` → `188 passed in 0.39s`. Updated README. No external side effects. Next exact slice: inventory the disclosures surface and fail closed until its required disclosures are explicitly handled.
- 2026-08-25 14:20 PDT — Progress on application-speed task 3: `njoyn_handler.py` now inventories the rendered Njoyn employment-disclosures surface, including each authorization/sponsorship radio control, and fail-closes it as an `employment_disclosures` manual gate until required truthful disclosures are explicitly handled. Strict RED→GREEN: `python -m pytest tests/test_njoyn_handler.py::test_inspect_html_inventories_njoyn_disclosures_and_fails_closed -v` initially failed with `page_type == application`, then passed (`1 passed`). Updated README; full suite: `python -m pytest tests -q` → `189 passed in 0.29s`. No external side effects. Next exact slice: inventory the disability surface and fail closed until its voluntary disclosure is explicitly handled.
- 2026-08-25 14:25 PDT — Progress on application-speed task 3: `njoyn_handler.py` now recognizes the rendered Njoyn voluntary-disability disclosure surface, inventories its native select with an exact heading label, and fail-closes it as a `disability_disclosure` manual gate until it is explicitly handled. Strict RED→GREEN: `python -m pytest tests/test_njoyn_handler.py::test_inspect_html_inventories_njoyn_disability_surface_and_fails_closed -v` initially failed on `page_type == application`, then passed (`1 passed`). Focused Njoyn suite: `5 passed`; updated README; full suite: `python -m pytest tests -q` → `190 passed in 0.31s`. No external side effects. Next exact slice: inventory the resume-upload surface and verify its attached filename by read-back.
- 2026-08-25 14:30 PDT — Progress on application-speed task 3: `njoyn_handler.py` now recognizes the `resume-upload` surface, inventories its native file input, and verifies the attached filename by exact `data-uploaded-file` read-back against the expected resume basename; the surface stays non-submitting and not safe to prepare. Strict RED→GREEN: `python -m pytest tests/test_njoyn_handler.py::test_inspect_html_inventories_njoyn_resume_upload_and_verifies_attached_filename -v` initially failed with `page_type == application`, then passed (`1 passed`). Updated README and verified the full suite: `python -m pytest tests -q` → `191 passed in 0.87s`. No external side effects. Next exact slice: inventory the parsed-profile surface and report parser-correction requirements from explicit mismatches.
- 2026-08-25 14:36 PDT — Progress on application-speed task 3: `njoyn_handler.py` now recognizes `parsed-profile` surfaces and reports only explicit `data-parser-mismatch` markers as a `parser_correction` manual gate, retaining the exact mismatch field identifiers so parsed data is never silently trusted. Strict RED→GREEN: `python -m pytest tests/test_njoyn_handler.py::test_inspect_html_reports_explicit_njoyn_parsed_profile_mismatches_for_correction -v` initially failed with `page_type == application`, then passed (`1 passed`). Focused Njoyn suite: `7 passed`; full suite: `python -m pytest tests -q` → `192 passed in 0.70s`. Updated README. No external side effects. Next exact slice: inventory the Njoyn referral surface and require the real two-step Social Media → Instagram selection state.
- 2026-08-25 14:42 PDT — Progress on application-speed task 3: `njoyn_handler.py` now inventories the rendered referral surface and reads selected native-option state, failing closed unless the real two-step selection is exactly `Social Media` → `Instagram`; unselected controls cannot be mistaken for typed completion. Strict RED→GREEN: `python -m pytest tests/test_njoyn_handler.py::test_inspect_html_requires_real_two_step_njoyn_referral_selection_state -v` initially failed with `page_type == application`, then passed (`1 passed`). Full suite: `python -m pytest tests -q` → `193 passed in 0.28s`. Updated README. No external side effects. Next exact slice: inventory the Njoyn questionnaire surface and fail closed on unknown required questions.
- 2026-08-25 14:48 PDT — Progress on application-speed task 3: `njoyn_handler.py` now recognizes rendered questionnaire surfaces, inventories their native controls, and fail-closes with an `unknown_required_questions` gate so no required answer is guessed or silently accepted. Strict RED→GREEN: `python -m pytest tests/test_njoyn_handler.py::test_inspect_html_inventories_njoyn_questionnaire_and_fails_closed_on_required_questions -v` initially failed with `page_type == application`, then passed (`1 passed in 2.42s`). Full suite: `python -m pytest tests -q` → `194 passed in 1.74s`. Updated README. No external side effects. Next exact slice: inspect Njoyn confirmation surfaces and extract exact confirmation evidence without treating a submit action as success.
- 2026-08-25 14:54 PDT — Completed application-speed task 3: `njoyn_handler.py` now extracts a rendered CGI/Njoyn `Reference:` identifier only after shared confirmation validation succeeds, returning `confirmation_reference_id` alongside the confirmation evidence while keeping `submission_enabled` behavior absent and `safe_to_prepare: false`. Strict RED→GREEN: `python -m pytest tests/test_njoyn_handler.py::test_inspect_html_extracts_verified_njoyn_confirmation_reference_without_submit_inference -v` initially failed with `KeyError: 'confirmation_reference_id'`, then passed (`1 passed`). Focused Njoyn suite: `10 passed`; full suite: `python -m pytest tests -q` → `195 passed in 1.06s`. Updated README. No external side effects. Next task: register CGI/Njoyn in the shared ATS registry and `prepare-job` dispatcher while preserving `submission_enabled: false` and unsupported-surface fail-closed behavior.
- 2026-08-25 15:00 PDT — Completed application-speed task 4: registered CGI/Njoyn (`njoyn.com`) in the shared ATS registry, so `prepare_job.prepare_saved_html(...)` now dispatches saved Njoyn listing surfaces through the handler while retaining the common `platform: njoyn`, `page_url`, `submission_enabled: false`, and fail-closed `safe_to_prepare: false` contract. Strict RED→GREEN: `python -m pytest tests/test_prepare_job.py::test_prepare_saved_html_dispatches_njoyn_listing_without_enabling_submission -v` initially failed with `ValueError: Unsupported ATS for URL`, then passed (`1 passed`). Full suite: `python -m pytest tests -q` → `196 passed in 0.29s`. Updated README. No external side effects. Next task: secure runtime credential adapter contract for account creation/sign-in that reads approved Keychain metadata without logging, serializing, or exposing secrets.
- 2026-08-25 15:04 PDT — Completed application-speed task 5: added `credential_adapter.py`, a secret-free Keychain-reference contract for approved ATS account flows. It verifies only approved service/account metadata via `security find-generic-password` without `-w`, returns availability plus a `runtime_only` reference, and rejects unapproved services before Keychain access; no credential value is read, logged, serialized, or exposed. Strict RED→GREEN: `python -m pytest tests/test_credential_adapter.py::test_resolve_keychain_reference_checks_metadata_without_exposing_a_secret -v` initially failed with `ModuleNotFoundError: No module named 'credential_adapter'`, then focused tests passed (`2 passed`). Full suite: `python -m pytest tests -q` → `198 passed in 0.30s`. Updated README. No external side effects. Next task: deterministic page answer-map executor that fills a complete known page in one bounded browser operation and returns field-by-field read-back evidence.

- 2026-08-25 15:09 PDT — Completed application-speed task 6: added `answer_map_executor.py`, a bounded known-page answer-map contract that invokes one page-level fill operation and returns exact field-by-field expected/actual read-back evidence, with a single all-fields verification verdict. Strict RED→GREEN: `python -m pytest tests/test_answer_map_executor.py::test_execute_known_page_fills_answer_map_once_and_returns_field_read_back_evidence -v` initially failed with `ModuleNotFoundError: No module named 'answer_map_executor'`, then passed (`1 passed`). Refactored to read each field once; re-verified focused test and full suite: `python -m pytest tests -q` → `199 passed in 0.26s`. Updated README. No external side effects. Next task: retry/recovery policy primitives: one normal attempt, one inspected recovery, then a stable blocker; never loop blindly or duplicate submission.
- 2026-08-25 15:15 PDT — Progress on application-speed task 7: added `retry_recovery.py` with a bounded recovery primitive that makes one normal attempt, permits exactly one post-interruption inspection, and returns inspector-confirmed completion without replaying a potentially submitted action. Strict RED→GREEN: `python -m pytest tests/test_retry_recovery.py::test_execute_once_uses_one_inspection_to_recover_without_replaying_normal_attempt -v` initially failed with `ModuleNotFoundError: No module named 'retry_recovery'`, then passed (`1 passed`). Full suite: `python -m pytest tests -q` → `200 passed in 0.27s`. Updated README. No external side effects. Next exact slice: return a stable blocker after a non-confirming recovery inspection, without a second normal attempt.
- 2026-08-25 15:20 PDT — Completed application-speed task 7: a non-confirming recovery inspection now returns a stable `blocked` result with its inspection reason after exactly one normal attempt and one inspection; it never replays a potentially submitted action. Strict RED→GREEN: `python -m pytest tests/test_retry_recovery.py::test_execute_once_returns_stable_blocker_after_nonconfirming_inspection -v` initially failed by re-raising `RecoverableAttemptError: connection interrupted`, then passed (`1 passed`). Full suite: `python -m pytest tests -q` → `201 passed in 0.30s`. Updated README. No external side effects. Next task: wire account/session reuse and learned tenant metadata into preparation plans so repeat applications skip rediscovery and account creation when authenticated state exists.
- 2026-08-25 15:26 PDT — Progress on application-speed task 8: `prepare_job.prepare_saved_html(...)` now carries matching learned tenant authenticated-state metadata plus a runtime-only session reference into a non-submitting preparation plan. Authenticated Njoyn tenants explicitly set `reuse_authenticated_session: true` and `account_creation_required: false`; credentials are neither read nor serialized. Strict RED→GREEN: `python -m pytest tests/test_prepare_job.py::test_prepare_saved_html_reuses_authenticated_learned_tenant_session_without_account_creation -v` initially failed with unexpected `tenant_metadata`, then passed (`1 passed`). Full suite: `python -m pytest tests -q` → `202 passed in 0.28s`. Updated README. No external side effects. Next exact slice: add a learned tenant metadata store/load contract that validates platform/tenant identity before it reaches preparation plans.
- 2026-08-25 15:32 PDT — Progress on application-speed task 8: added `tenant_metadata.py`, a versioned learned-tenant loader that validates exact page hostname, ATS platform, hostname-derived tenant identity, authenticated-state type, and `runtime-only:` session-reference format before returning the secret-free metadata that may reach a preparation plan. Strict RED→GREEN: `python -m pytest tests/test_prepare_job.py::test_load_tenant_metadata_returns_matching_authenticated_njoyn_tenant_only -v` initially failed with `ModuleNotFoundError: No module named 'tenant_metadata'`, then passed (`1 passed`). Full suite: `python -m pytest tests -q` → `203 passed in 0.73s`. Updated README. No external side effects. Next exact slice: wire the validated learned-tenant metadata loader into the non-submitting `prepare_job.py` CLI without serializing secrets.
- 2026-08-25 15:36 PDT — Completed application-speed task 8: `prepare_job.py --tenant-metadata <path>` now resolves the page handler, loads only its hostname/platform-validated learned tenant record, and passes the already secret-free runtime-only session metadata into the non-submitting plan. An authenticated CGI/Njoyn record produces explicit session-reuse/account-creation-false evidence; no credential value is read, written, or serialized. Strict RED→GREEN: `python -m pytest tests/test_prepare_job.py::test_main_loads_validated_tenant_metadata_without_serializing_secrets -v` initially failed because `--tenant-metadata` was unrecognized, then passed (`1 passed`). Focused prepare-job suite: `8 passed`; full suite: `python -m pytest tests -q` → `204 passed in 0.28s`. Updated README. No external side effects. Next task: add non-submitting vertical E2E fixtures for CGI/Njoyn through source → queue → answer coverage → plan → resume/parser repair evidence → confirmation artifact.

- 2026-08-25 15:40 PDT — Progress on application-speed task 9: added `run_njoyn_fixture_flow` to carry a sanitized CGI/Njoyn candidate through idempotent queueing, profile-backed answer coverage, non-submitting resume-plan preparation, parsed-profile review evidence, and a sanitized confirmation artifact. It verifies the attached `Resume.pdf`, preserves `submission_enabled: false`, and neither submits nor mutates tracker/notification systems. Strict RED→GREEN: `python -m pytest tests/test_fixture_e2e.py::test_run_njoyn_fixture_flow_prepares_resume_plan_and_parser_review_evidence -v` initially failed with missing `run_njoyn_fixture_flow`, then passed (`1 passed`); full suite: `python -m pytest tests -q` → `205 passed in 0.32s`. Updated README. Next exact slice: add an explicit parser-mismatch repair-evidence fixture flow that remains blocked until the recorded correction is resolved.

- 2026-08-25 15:44 PDT — Progress on application-speed task 9: added a sanitized CGI/Njoyn parsed-profile mismatch fixture and field-only parser-repair evidence to `run_njoyn_fixture_flow`. Explicit parser mismatches now produce a stable blocked result with required/unresolved field identifiers and `safe_to_continue: false`; the non-submitting, tracker/notification-free invariant is preserved. Strict RED→GREEN: `python -m pytest tests/test_fixture_e2e.py::test_run_njoyn_fixture_flow_blocks_parser_mismatch_until_correction_is_recorded -v` initially failed with `KeyError: 'parser_repair_evidence'`, then passed (`1 passed`). Full suite: `python -m pytest tests -q` → `206 passed in 0.29s`. Updated README. Next exact slice: accept field-identified recorded parser corrections only when they resolve every explicit mismatch, while retaining fail-closed behavior for partial or unrelated corrections.

- 2026-08-25 15:49 PDT — Progress on application-speed task 9: `run_njoyn_fixture_flow` now accepts field-identified recorded parser corrections and emits `resolved` parser-repair evidence only after every explicit mismatch is covered; resolved/unresolved field IDs remain read-back evidence and the flow remains non-submitting. Strict RED→GREEN: `python -m pytest tests/test_fixture_e2e.py::test_run_njoyn_fixture_flow_accepts_all_recorded_parser_corrections -v` initially failed with `TypeError: run_fixture_flow() got an unexpected keyword argument 'parser_corrections'`, then passed (`1 passed`). Full suite: `python -m pytest tests -q` → `207 passed in 0.42s`. Updated README. No external side effects. Next exact slice: reject partial or unrelated recorded parser corrections while preserving the blocked evidence contract.

- 2026-08-25 15:54 PDT — Completed application-speed task 9 parser-repair safety slice: `run_njoyn_fixture_flow` now exposes unrelated field IDs as `rejected_corrections` and leaves the explicit parser mismatch blocked; only the required field IDs can resolve parser repair. Strict RED→GREEN: `python -m pytest tests/test_fixture_e2e.py::test_run_njoyn_fixture_flow_rejects_unrelated_recorded_parser_correction -v` initially failed because the expected `rejected_corrections` read-back evidence was absent, then passed (`1 passed`). Full suite: `python -m pytest tests -q` → `208 passed in 0.31s`. Updated README. No external side effects. Next task: add the production-readiness benchmark/audit for learned-ATS preparation under 5 minutes and verified submission under 10 minutes while preserving all safety invariants.
- 2026-08-25 16:02 PDT — Completed application-speed task 10: extended `production_readiness.py` with an optional persisted CGI/Njoyn benchmark-evidence audit. It requires preparation below 300 seconds and fully verified submission below 600 seconds while preserving disabled submission/external side effects and parser-repair, Review, confirmation, tracker-read-back, and Discord-read-back evidence; the CLI accepts `--learned-ats-benchmark` and fails readiness if any supplied benchmark condition is unmet. Strict RED→GREEN: `python -m pytest tests/test_production_readiness.py::test_audit_accepts_verified_learned_ats_speed_evidence_with_safety_invariants -v` initially failed with unexpected `learned_ats_benchmark`, then passed; CLI persistence test initially failed because `--learned-ats-benchmark` was unrecognized, then passed. Focused audit suite: `4 passed`; full suite: `python -m pytest tests -q` → `210 passed in 0.29s`; `git diff --check` passed. Updated README. No external side effects. Application-speed queue complete.
- 2026-08-25 19:35 PDT — Recovered the interrupted Amazon-monitor integration slice already present in the worktree: `amazon_sync.py` keeps the default path non-mutating, deduplicates official monitor hits, stages MAANGO manual-only rows, and only performs tracker append/queue enqueue through explicit injected or `--commit` paths. Focused regression suite: `python -m pytest tests/test_amazon_sync.py -q` → `3 passed`; full suite: `python -m pytest tests -q` → `213 passed in 0.35s`; `git diff --check` passed. The unrelated generated OCR artifact remains untracked. No application, tracker, queue, or notification mutation occurred in this recovery run. Application-speed queue complete.

## Safe live-execution engineering queue — completed

Explicit continuation priority from Kevin: connect the verified action/ATS contracts to a fast, scoped live browser executor while ensuring automation never steals focus, opens native macOS panels, uses raw OS coordinates, weakens truthfulness, or submits without confirmation evidence.

1. Replace the untracked `cdp_inspect.py` prototype with a tested reusable scoped-CDP transport that binds an exact Chrome target, performs read-only state/snapshot operations, and forbids `osascript`, System Events, `cliclick`, pyautogui, or global desktop coordinates.
2. Wire `browser_actions.py` primitives into one bounded CDP page executor with exact post-action read-back evidence and stale-target detection.
3. Add a universal OCR/visual escalation contract shared by every ATS: DOM/AX attempt → one verified failure → scoped screenshot/OCR → one inspected retry → stable blocker; no raw desktop fallback.
4. Add a non-submitting CGI/Njoyn live executor that consumes handler plans, batches known-page answers, verifies parser repairs, and stops before final submit by default.
5. Add equivalent non-submitting live executors for Workday, Greenhouse, Lever, and Oracle using the same action/evidence contract rather than ATS-specific ad hoc clicks.
6. Add authenticated-session and tenant reuse through runtime-only references, with explicit account/login/verification gates and no credential serialization.
7. Add page-level recovery journaling so interrupted clicks/uploads/saves resume from verified state and never replay a possibly submitted action.
8. Add timing telemetry for discovery, login, upload, form fill, parser repair, Review, confirmation, tracker read-back, and Discord read-back without logging PII.
9. Add sanitized browser-integration canaries for all learned ATS executors, including Retina scaling, stale focus, hidden controls, overlays, and unexpected native-window rejection.
10. Run a final production-readiness and speed audit proving learned-ATS preparation under five minutes and verified submission under ten minutes while all safety invariants remain enabled.

Implement strictly in order with RED → GREEN → REFACTOR. Each run completes at most one coherent slice, runs the focused test and `python -m pytest tests -q`, updates docs, appends verified evidence here, and commits only files belonging to that slice. Treat `cdp_inspect.py` as an untrusted prototype for task 1: preserve it until a failing test defines its replacement, then either migrate or remove it explicitly. Generated cron/OCR JSON artifacts are runtime-only and must never be committed. Build mode forbids real applications, tracker writes, messages, email writes, credential mutations, account changes, or submissions.

- 2026-08-27 00:01 PDT — Completed safe live-execution task 1: replaced the untracked `cdp_inspect.py` prototype with `scoped_cdp.py`, a reusable exact-target CDP transport that resolves only a current `type == page` target through `/json/list`, binds its exact websocket URL, and exposes fixed read-only `Runtime.evaluate` snapshot reads (URL, title, body text, HTML). The public binding explicitly permits only `Runtime.evaluate`; no navigation, input, uploads, native desktop automation, or global-coordinate APIs exist. Strict RED→GREEN: `python -m pytest tests/test_scoped_cdp.py -v` initially failed with `ModuleNotFoundError: No module named 'scoped_cdp'`, then with missing explicit allowed-method evidence, and passed (`2 passed`). Next task: wire `browser_actions.py` primitives into one bounded CDP page executor with exact post-action read-back evidence and stale-target detection.
- 2026-08-27 00:05 PDT — Progress on safe live-execution task 2: added `cdp_page_executor.py` with the first bounded exact-target executor slice. `replace_text` requires a fresh, trusted read-only snapshot whose target ID and URL exactly match the caller’s declared binding before it invokes the existing post-action read-back contract; untrusted state blocks before mutation. Strict RED→GREEN: `python -m pytest tests/test_cdp_page_executor.py::test_executor_replaces_text_only_after_exact_target_readback -v` initially failed with `ModuleNotFoundError: No module named 'cdp_page_executor'`, then passed; the untrusted-snapshot regression initially failed with `Failed: DID NOT RAISE`, then passed. Focused suite: `2 passed`; full suite: `python -m pytest tests -q` → `217 passed in 1.14s`. No external side effects.
- 2026-08-27 00:11 PDT — Completed safe live-execution task 2: extended the same bounded executor with `native_select`, requiring the identical fresh exact-target read-back before selecting a real native option and returning target-bound selected-option evidence. Strict RED→GREEN: `python -m pytest tests/test_cdp_page_executor.py::test_executor_selects_native_option_only_after_exact_target_readback -v` failed as expected with `AttributeError: 'CDPPageExecutor' object has no attribute 'native_select'`, then passed after the minimal executor method. Focused suite: `3 passed`; full suite: `python -m pytest tests -q` → `218 passed in 0.31s`. No external side effects. Next exact task: add a universal DOM/AX → scoped screenshot/OCR escalation contract with one retry and stable blocker.
- 2026-08-27 00:15 PDT — Completed safe live-execution task 3: added reusable `visual_escalation.py`, a platform-neutral contract that calls DOM/AX once, escalates only after a returned verified failure to one exact-page scoped screenshot/OCR observation, performs one inspected retry, and otherwise returns a stable blocker. The module has no raw desktop input surface; blank/missing screenshot scope also fails closed without retry. Strict RED→GREEN: `python -m pytest tests/test_visual_escalation.py::test_visual_escalation_retries_once_after_verified_dom_ax_failure -v` initially failed with `ModuleNotFoundError: No module named 'visual_escalation'`, then passed. Focused suite: `2 passed`; full suite: `python -m pytest tests -q` → `220 passed in 0.29s`. No external side effects. Next exact task: add a non-submitting CGI/Njoyn executor that consumes handler plans and stops before final submit.
- 2026-08-27 00:19 PDT — Completed safe live-execution task 4: added `njoyn_live_executor.py`, which accepts only an Njoyn handler plan with `submission_enabled: false`, batches known-page answers through the existing exact read-back contract, reads and verifies recorded parser repairs against the plan, and returns `ready_for_human_review` with mandatory `stop_before_submit`. It exposes no submit operation. Strict RED→GREEN: `python -m pytest tests/test_njoyn_live_executor.py::test_njoyn_executor_batches_answers_verifies_parser_repairs_and_stops_before_submit -v` initially failed with `ModuleNotFoundError: No module named 'njoyn_live_executor'`, then passed. Focused suite: `1 passed`; full suite: `python -m pytest tests -q` → `221 passed in 0.31s`. No external side effects. Next exact task: add equivalent non-submitting executors for Workday, Greenhouse, Lever, and Oracle through the shared action/evidence contract.
- 2026-08-27 00:23 PDT — Completed safe live-execution task 5: added `standard_ats_live_executor.py`, a single shared non-submitting executor for Workday, Greenhouse, Lever, and Oracle. It accepts only a supported handler plan with `submission_enabled: false`, batches approved known-page answers through the same exact read-back evidence contract, and invariably returns `stop_before_submit` for human Review; it has no submit operation or ATS-specific click path. Strict RED→GREEN: the four-platform parametrized focused test initially failed with `ModuleNotFoundError: No module named 'standard_ats_live_executor'`, then passed (`4 passed`). Full suite: `python -m pytest tests -q` → `225 passed in 0.29s`; `git diff --check` passed. No external side effects. Next exact task: add runtime-only authenticated-session and tenant reuse with explicit gates and no credential serialization.
- 2026-08-27 00:27 PDT — Completed safe live-execution task 6: added `session_gate.py`, which validates only tenant name, boolean authenticated state, and a `runtime-only:` reference. It returns authenticated reuse only when already authenticated; otherwise it stops at the explicit `login_or_identity_verification_required` human gate. It never accepts, reads, or serializes a credential. Strict RED→GREEN: `python -m pytest tests/test_session_gate.py::test_session_gate_reuses_authenticated_runtime_reference_without_serializing_credentials -v` initially failed with `ModuleNotFoundError: No module named 'session_gate'`, then passed. Focused suite: `2 passed`; full suite: `python -m pytest tests -q` → `227 passed in 0.32s`. No external side effects. Next exact task: add page-level recovery journaling that never replays an uncertain submit.
- 2026-08-27 00:30 PDT — Completed safe live-execution task 7: added `page_recovery.py`, a durable JSONL journal for click/upload/save/submit evidence. It resumes only from a verified latest action; critically, an interrupted/uncertain submit produces `inspect_confirmation_without_replay` rather than any replay route. Strict RED→GREEN: `python -m pytest tests/test_page_recovery.py::test_recovery_journal_blocks_replay_after_uncertain_submit -v` initially failed with `ModuleNotFoundError: No module named 'page_recovery'`, then passed. Focused suite: `1 passed`; full suite: `python -m pytest tests -q` → `228 passed in 0.29s`. No external side effects.
- 2026-08-27 00:33 PDT — Completed safe live-execution task 8: added PII-free `timing_telemetry.py` with a closed stage vocabulary covering discovery, login, upload, form fill, parser repair, Review, confirmation, tracker read-back, and Discord read-back. It emits only stage and non-negative elapsed seconds. Strict RED→GREEN: `python -m pytest tests/test_timing_telemetry.py -v` initially failed with `ModuleNotFoundError: No module named 'timing_telemetry'`, then passed. Full suite: `python -m pytest tests -q` → `229 passed in 0.29s`. No external side effects.
- 2026-08-27 00:40 PDT — Completed safe live-execution task 9: added sanitized `browser_integration_canary.py` for Njoyn, Workday, Greenhouse, Lever, and Oracle. It runs only against supplied non-sensitive surface facts with `submission_enabled: false`, passes an exact-current visible surface, and fails closed for invalid Retina scaling, stale focus, hidden controls, overlays, and unexpected native-window detection; it has no browser, desktop, or submit API. Strict RED→GREEN: `python -m pytest tests/test_browser_integration_canary.py::test_browser_canary_accepts_sanitized_exact_target_surface -v` initially failed with `ModuleNotFoundError: No module named 'browser_integration_canary'`, then passed. Focused suite: `10 passed`; full suite: `python -m pytest tests -q` → `239 passed in 0.31s`. No external side effects.
- 2026-08-27 00:45 PDT — Completed safe live-execution task 10: strengthened the final read-only `production_readiness.py` audit so provided browser evidence must pass for all five learned executors (Njoyn, Workday, Greenhouse, Lever, Oracle) with submission disabled, in addition to all prior dry-run, fixture-flow, speed, parser repair, Review, confirmation, tracker-read-back, and Discord-read-back invariants. Strict RED→GREEN: `python -m pytest tests/test_production_readiness.py::test_audit_requires_passing_canaries_for_every_learned_executor -v` initially failed with `TypeError: build_audit() got an unexpected keyword argument 'browser_canaries'`, then passed. Focused suite: `5 passed`; full suite: `python -m pytest tests -q` → `240 passed in 0.32s`. The audit evaluates persisted evidence only; build mode performed no actual submit, tracker write, or notification. Queue complete.

## Production operator engineering queue — completed

Explicit continuation priority from Kevin: turn the tested contracts into one usable, fast, approval-gated production operator. The current modules prove isolated behavior but still depend on injected page seams and persisted evidence assembled by hand. This queue closes the gap without weakening browser scoping, truthful answers, manual gates, Review authority, one-shot submission, or reconciliation.

1. Add a mutable exact-target CDP page adapter implementing the bounded executor page protocol for text, native selects, checked controls, file inputs, and read-back, while forbidding navigation, raw desktop input, global coordinates, hidden-control mutation, and stale-target reuse.
2. Add a single non-submitting `prepare-live-job` command that binds an exact target ID, verifies URL/company/role/requisition, dispatches the ATS handler, runs answer coverage, applies only approved known answers, and persists a sanitized Review-ready evidence bundle.
3. Add an authoritative Review reconciler that compares server-rendered values and exact `Resume.pdf` against profile/resume facts, parser-repair evidence, required questions, and target identity; unresolved differences remain human-required.
4. Add expiring single-use submission authorization bound to job ID, target ID, URL, requisition, Review-evidence hash, and actor. Any target or Review drift invalidates approval before submission.
5. Add a one-shot submit operator that consumes authorization once, journals intent before action, clicks one exact submit control, and uses confirmation inspection without replay after interruption; it must reject CAPTCHA, assessment, email/identity verification, and MAANGO without approval.
6. Add confirmation extraction and candidate-home/application-list reconciliation for all learned ATS handlers, requiring exact company/role/requisition and explicit submitted state.
7. Add a post-submit transaction coordinator that appends the tracker only after portal confirmation, verifies Sheets read-back, sends Discord only after tracker verification, verifies Discord read-back, and emits a sanitized final artifact; partial failures remain resumable and never duplicate submit.
8. Add tenant-specific field maps and conditional-step adapters for Njoyn, Workday, Greenhouse, Lever, and Oracle so the production command uses learned controls rather than rediscovery on every application.
9. Run local real-Chrome CDP integration tests against sanitized static ATS pages for prepare → Review → approval → one-shot submit → confirmation, including stale target, overlays, Retina, OCR escalation, interrupted submit, and restart recovery.
10. Add the production operator CLI/runbook, operational health gates, timing report, and final audit. Demonstrate a fully local sanitized learned-ATS run under the timing targets before any real application is authorized.

Implement strictly in order with RED → GREEN → REFACTOR. Work continuously through coherent vertical slices. Every slice must show expected RED, focused GREEN, full-suite GREEN, documentation/evidence, and a separate descriptive commit. Build and integration-test mode must not submit a real application, mutate the live tracker, send a real message/email, read or change credentials/accounts, or use production ATS data. Never use `osascript`, System Events, `cliclick`, pyautogui, Quartz/global coordinates, focus stealing, Apple-menu/System Settings/About This Mac interaction, or raw desktop input. Browser actions require exact-target CDP, fresh target/URL read-back, visible enabled controls, bounded OCR observation, and post-action evidence.

### Verified engineering evidence

- Task 1, mutable CDP field adapter: RED observed for each missing operation (`replace_text` module missing; then `select_option`, `set_checked`, and `cdp_upload` absent). GREEN: focused adapter tests passed; full suite passed `244 passed`. The adapter only evaluates exact-bound pages, re-reads URL before every operation/read-back, gates controls on visible/enabled state, and limits mutation to text/native select/checked/file-input CDP operations. No navigation, desktop input, global coordinates, or production data path was added.
- Next exact slice: wire this adapter into a non-submitting `prepare-live-job` command with exact target identity verification and sanitized evidence persistence (Task 2).
- Task 2, first vertical slice: RED observed because `prepare_live_job` did not exist. GREEN: focused exact-binding/identity and target-drift tests passed; full suite passed `246 passed`. The new non-submitting seam reads one bound target snapshot, rejects target/URL or company/role/requisition drift before handler dispatch, and retains only sanitized Review evidence and answer coverage. It has no CDP connection setup, credential access, tracker, notification, or submit path.
- Next exact slice: add approved-known-answer application plus its post-action evidence to the Task 2 seam, then expose the guarded CLI only over a supplied exact CDP binding.
- Task 2, approved-answer vertical slice: RED observed as `prepare_live_job` rejected the new `approved_answers` and `apply_known` arguments. GREEN: focused live-preparation tests and full suite passed `246 passed`. The seam now executes only an explicitly supplied approved answer map and refuses to emit Review evidence if any field read-back is unverified. Empty maps produce explicit empty verified evidence; it cannot infer or apply unknown answers.
- Next exact slice: add a guarded CLI that constructs the preparation seam from a newly exact-bound local CDP page, with no persisted credentials or external side effects.
- Task 2, fresh-binding slice: RED observed because `ScopedCDPTransport` had no mutable exact-target binding. GREEN: focused scoped transport tests and full suite passed `247 passed`. The new context-managed binding freshly resolves a `type == page` target by exact ID and hands the same connection only to the bounded mutable adapter; context exit closes the exact connection. It cannot navigate or enumerate unrelated targets after binding.
- Next exact slice: add the CLI adapter from this freshly bound page to sanitized preparation/evidence persistence.
- Task 2, guarded CLI completion slice: RED observed because `prepare_live_job.main` did not exist. GREEN: `python -m pytest tests/test_prepare_live_job.py::test_cli_fresh_binds_applies_explicit_answers_and_persists_sanitized_evidence -v` passed, and the full suite passed `248 passed`. The CLI accepts only a loopback CDP origin, freshly exact-binds one mutable target, dispatches page HTML through the ATS preparation seam, applies only the explicit selector/value map with mandatory per-field target-bound read-back, closes the binding, strips all profile/answer values, and writes evidence only after successful non-submitting preparation.
- Next exact slice: start Task 3 with an authoritative Review reconciler that rejects any server-rendered/profile, exact `Resume.pdf`, parser-repair, required-question, or target-identity mismatch as human-required.
- Task 3, authoritative Review reconciler: RED observed because `review_reconciler` did not exist. GREEN: `python -m pytest tests/test_review_reconciler.py -v` passed `2 passed`; the full suite passed `250 passed`. The pure reconciler requires exact prepared/server target identity, exact server/profile field equality, independently verified `Resume.pdf` basename and SHA-256, verified required parser repairs, and answered/read-back-verified required questions. It emits only sanitized identifiers/booleans, preserves every difference as `human_required`, generates a canonical Review evidence hash, and explicitly grants no submit authority.
- Next exact slice: start Task 4 with expiring single-use authorization bound to job ID, exact target/URL/requisition, authoritative Review hash, and actor, rejecting drift and replay.
- Task 4, expiring single-use submission authorization: RED observed because `submission_authorization` did not exist. GREEN: `python -m pytest tests/test_submission_authorization.py -v` passed `3 passed`; the full suite passed `253 passed`. The SQLite store recomputes the authoritative Review hash, binds job/target/URL/requisition/hash/actor, persists only a token digest, consumes atomically once, rejects expiry/replay, and commits permanent invalidation on any binding drift. It performs no browser action or external mutation.
- Next exact slice: start Task 5 with a one-shot exact submit operator that consumes authorization, journals intent before one visible/enabled click, rejects all mandatory gates, and inspects confirmation without replay after interruption.
- Task 5, guarded one-shot submit operator: RED observed because `one_shot_submit` did not exist. GREEN: `python -m pytest tests/test_one_shot_submit.py -v` passed `7 passed`; the full suite passed `260 passed`. Tests prove gate/control checks precede consumption, submit intent precedes the one exact click, replay cannot reach another click, and interruption only inspects confirmation. CAPTCHA, assessment, email/identity verification, unapproved MAANGO, and hidden/non-exact controls fail closed. Only sanitized fake pages and local SQLite were used; no real submission occurred.
- Next exact slice: start Task 6 with learned-ATS confirmation extraction plus candidate-home/application-list reconciliation requiring exact company, role, requisition, and explicit submitted state.
- Task 6, learned-ATS confirmation and portal reconciliation: RED observed because `confirmation_reconciliation` did not exist. GREEN: `python -m pytest tests/test_confirmation_reconciliation.py -v` passed `6 passed` across Greenhouse, Workday, Lever, Oracle, and Njoyn; the full suite passed `266 passed`. Learned handlers must prove confirmation, requisition must match URL/reference, and one unique portal record must exactly match platform/company/role/requisition with both explicit submitted indicators. Identity/state/ambiguity drift remains human-required, and raw HTML is retained only as a hash. Only sanitized fixtures were used.
- Next exact slice: start Task 7 with a resumable post-submit coordinator enforcing portal confirmation → tracker append/read-back → Discord send/read-back, with no duplicate submit or downstream replay.
- Task 7, resumable post-submit transaction: RED observed because `post_submit_transaction` did not exist. GREEN: `python -m pytest tests/test_post_submit_transaction.py -v` passed `4 passed`; the full suite passed `270 passed`. Tests prove portal confirmation precedes tracker append/read-back, tracker verification precedes Discord send/read-back, and partial tracker or Discord failures resume by read-back without duplicate append/send. SQLite stores only stable hashes, attempt/verification flags, and receipt IDs; the sanitized final artifact contains no tracker/message payload and the coordinator exposes no submit path.
- Next exact slice: start Task 8 with versioned tenant-specific field maps and conditional-step adapters for all five learned ATS platforms, rejecting unknown tenant/control drift instead of rediscovering controls.
- Task 8, learned tenant field maps and conditional steps: RED observed because `tenant_field_maps` and the CLI `--platform/--step` path did not exist. GREEN: `python -m pytest tests/test_tenant_field_maps.py -v` passed `8 passed`; the full suite passed `278 passed`. Versioned exact host/path maps cover sanitized Greenhouse, Workday, Lever, Oracle, and Njoyn tenants; semantic keys resolve fixed operations/selectors, raw selectors and control drift fail closed, and Workday/Njoyn transitions require verified fields plus explicit parser/question conditions. The default `prepare_live_job` path now dispatches the handler, verifies platform, applies mapped actions with fresh target read-back, and strips values from evidence. Unsupported Oracle combobox and credential fields remain human-required.
- Next exact slice: start Task 9 with a real local Chrome CDP integration harness over sanitized static ATS pages covering prepare → Review → approval → one-shot submit → confirmation plus stale/overlay/Retina/OCR/interruption/restart cases.
- Task 9, sanitized real-Chrome exact-CDP integration: RED observed because `local_cdp_operator` did not exist. GREEN: `python -m pytest tests/test_local_cdp_operator.py -q` passed `1 passed` against Chrome-for-Testing, and the full suite passed `279 passed`. One marker-checked local static page runs through prepare → Review → expiring approval → one-shot submit → learned-handler confirmation → portal reconciliation → local tracker/Discord read-backs. Dedicated process-local CDP pipes avoid a debugging listener; every field and target is freshly read back. The test also proves Retina scaling, overlay rejection before mutation, exact-page screenshot/OCR observation, post-click interruption recovery with submit count exactly one, process-session reattachment, token replay denial, and stale-URL rejection. All identity/resume/downstream data and adapters are sanitized/local.
- Next exact slice: start Task 10 with one production operator CLI that composes the guarded stages, explicit operational health gates, PII-free timing output, and a final audit; prove a fully local sanitized learned-ATS run stays under the preparation/submission timing targets.
- Task 10, production operator proof CLI, health/timing gates, and final audit: RED observed because `production_operator` did not exist. GREEN: `python -m pytest tests/test_production_operator.py -q` passed `6 passed`; the Task 9/10 integration set passed `7 passed`, and the full suite passed `285 passed`. `local-demo` refuses to start without explicit sanitized-submit approval, then uses all seven versioned Greenhouse controls on the exact static fixture, authoritative Review, expiring single-use authorization, one submit, learned confirmation/portal proof, and in-memory tracker/Discord read-backs. It emits only boolean/count evidence and PII-free stage timings, proves preparation below 300 seconds and verified local submission below 600 seconds, strips answers/tokens/paths, and keeps real authorization/submission false. The independent `audit` command fails closed on health, timing, side-effect, or replay drift. No live mode or production adapter exists.
- Next exact slice: queue implementation is complete; run the final full-suite, whitespace, forbidden-automation, executable-source, commit-history, artifact, and deliberate-worktree audit, then document the read-only `.git` handoff if it remains enforced.
- Final queue audit: `python -m pytest tests -q` passed `285 passed`; both tracked and staged whitespace checks passed; executable-source scans found no prohibited desktop-automation command/API usage; the only broad-text match is a safety docstring denying raw desktop input. A standalone sanitized Chrome run completed preparation in `0.460s` and verified local submission in `0.035s`, with submit count one, all external/production side effects false, and the final audit passing. The isolated Git mirror is clean and contains nine ordered task commits after `5096ed1`; `runtime/operator-queue-complete.bundle` is the complete-history handoff. The environment's real `.git` remained read-only, so its index/branch was not mutated.
- Next exact slice: no engineering queue item remains. When native `.git` write access is available, fetch the verified mirror/bundle into a temporary branch and fast-forward `main`, then confirm native `git status --short` is empty; do not recreate or squash the ordered task commits.

## Unified live-production CLI queue — completed

Explicit continuation priority from Kevin: expose the completed production-operator architecture as one guarded real-ATS command instead of requiring an agent to manually compose modules. Development remains fixture/local-only and must not submit a real application or mutate external systems.

1. Add a versioned live-run manifest binding mode, job/queue ID, exact target ID/URL, company, role, requisition, platform, tenant, profile/resume evidence, manual-gate state, and runtime paths; reject unknown fields, production mode without explicit enablement, and identity drift.
2. Add `production_operator live prepare` over Kevin's existing Chrome CDP endpoint: discover no unrelated tabs, bind only a caller-supplied target ID, run health/canary/handler/coverage/field-map preparation, upload exact profile-selected `Resume.pdf`, and persist sanitized Review evidence.
3. Add `live review` to extract the server-rendered Review surface through learned ATS adapters and invoke the authoritative reconciler; output only the Review hash, verified field identifiers, blockers, and exact job identity.
4. Add `live authorize` that requires an explicit actor/approval input, MAANGO authorization when applicable, authoritative Review hash, short expiry, and runtime-only token delivery; never print or persist the raw token outside the protected runtime handoff.
5. Add `live submit` that rebinds the exact target, rechecks health/gates/identity/Review hash, consumes authorization once, journals intent, submits once, and returns confirmation-inspection state without replay. Development tests use only sanitized local Chrome.
6. Add learned live confirmation/Candidate Home readers for Njoyn, Workday, Greenhouse, Lever, and Oracle, with exact submitted-state reconciliation and stable human-required fallbacks when a tenant lacks a verified reader.
7. Add real tracker and Discord transaction adapters behind explicit commit mode, using idempotency keys and authenticated read-back. Default/integration tests inject local adapters and cannot touch external services.
8. Add `live resume/status` for interrupted prepare, Review, submit, confirmation, tracker, and Discord stages; never repeat an uncertain submit or already verified downstream mutation.
9. Add one local real-Chrome CLI integration covering the complete command sequence and failure matrix, plus a non-mutating normal-Chrome preflight that proves exact-target attachment without editing any production page.
10. Update OPERATIONS.md and the job-application skill with the exact live command/runbook, approval semantics, rollback/recovery, health checks, and a final release audit. Real-live mode remains disabled unless every gate is explicit and verified.

Implement continuously in strict RED → GREEN → REFACTOR order, one committed vertical slice at a time. Run focused tests, `python -m pytest tests -q`, `git diff --check`, forbidden-automation scans, and update SPRINT evidence for every slice. Never use `osascript`, System Events, `cliclick`, pyautogui, global coordinates, focus stealing, or native macOS UI. Never submit a real application, mutate the live tracker, send a real message/email, or read/change credentials/accounts during this build run. Use exact-target CDP, local sanitized Chrome fixtures, injected adapters, explicit authorization, and no-replay recovery.

### Verified engineering evidence

- 2026-08-27 02:36 PDT — Completed unified CLI task 1: added `live_run_manifest.py` with a closed v1 schema binding mode, job/queue ID, exact target ID/URL, job identity, learned platform/tenant, independently verified profile and exact `Resume.pdf` evidence, explicit manual-gate/MAANGO state, and unique absolute runtime paths. RED: the focused manifest load test failed with `ModuleNotFoundError: No module named 'live_run_manifest'`. GREEN: `python -m pytest tests/test_live_run_manifest.py -q` → `2 passed`; full suite `python -m pytest tests -q` → `287 passed in 1.32s`. Unknown/missing fields, caller-unenabled `production_live`, and fresh observed identity drift fail closed; the manifest grants no browser, external-service, or submission authority. Next exact task: add `production_operator live prepare` over one caller-supplied exact CDP target.
- 2026-08-27 02:41 PDT — Completed unified CLI task 2: added `production_operator live prepare` with injectable local test seams and an exact-page `inspect_safety_surface` observation. RED: the focused CLI test failed with `TypeError: main() got an unexpected keyword argument 'live_transport_factory'`. GREEN: the focused test passed; full suite `python -m pytest tests -q` → `288 passed in 1.47s`. The stage accepts only loopback CDP, never auto-selects or navigates tabs, exact-binds the manifest target, verifies browser health/canary/handler/coverage/tenant/map/identity, hashes the profile, preflights and uploads only its exact selected `Resume.pdf`, requires read-back for every mapped action, and persists only sanitized preparation evidence. The development test used an injected page and sanitized local files; no ATS, credential, tracker, or notification service was accessed. Next exact task: add `live review` with learned server-Review extraction and authoritative reconciliation.
- 2026-08-27 02:45 PDT — Completed unified CLI task 3: added `live review` plus `live_review_reader.py` for fresh exact-target platform Review observation, with explicit learned reader seams for all five registered platforms and a conservative versioned-map fallback. RED: the focused CLI test failed because `review` was not a valid live subcommand. GREEN: the focused Review test passed; full suite `python -m pytest tests -q` → `289 passed in 1.14s`. The stage revalidates preparation/manifest/handler/tenant identity, derives expected selectors from semantic approved answers, and invokes the authoritative reconciler. It persists no values, paths, or resume digest; stdout contains only the canonical Review hash, verified identifiers, blockers, and exact job identity. Missing reader evidence remains human-required and exits nonzero. No submission or external adapter is reachable. Next exact task: add explicit actor/approval-gated `live authorize` with protected runtime-only token handoff.
- 2026-08-27 02:48 PDT — Completed unified CLI task 4: added `live authorize` requiring an explicit actor, exact retyped canonical Review hash, explicit 1–600 second expiry, blocker-free authority, matching job identity, cleared manual gates, and separate MAANGO approval when applicable. RED: the focused test failed because `production_operator.main` had no `live_clock`/authorize path. GREEN: two focused tests passed; full suite `python -m pytest tests -q` → `291 passed in 1.18s`. The raw token is created once in a mode-`0600` runtime handoff, never printed or added to Review, and absent from SQLite bytes because the store retains only its digest. Existing handoffs fail closed. The MAANGO test stops before issuance without the second approval. No browser, ATS, tracker, notification, or credential service was touched. Next exact task: add revalidated one-shot `live submit` with token consumption and no-replay confirmation inspection.
- 2026-08-27 02:52 PDT — Completed unified CLI task 5: added `live submit`, protected handoff validation, exact learned submit-control observation/activation, and conservative confirmation inspection. RED: the focused test failed because `submit` was not a valid live subcommand. GREEN: the focused one-shot/replay test passed; full suite `python -m pytest tests -q` → `292 passed in 1.09s`. The stage rechecks CDP health, exact handler/tenant identity, gates/MAANGO, profile/resume evidence, and a freshly reconciled Review hash before atomic token consumption. It journals intent before one visible/enabled/unique button activation, retires the consumed handoff, returns confirmation-inspection state without claiming reconciliation, and makes a repeated command fail before a second click. Only injected sanitized page evidence was used; no real submission or downstream effect occurred. Next exact task: add learned live confirmation and Candidate Home readers for all five ATS platforms.
- 2026-08-27 02:57 PDT — Completed unified CLI task 6: added `live_confirmation_reader.py`, a closed verified-reader registry for Njoyn/CGI, Workday/fixture, Greenhouse/fixture, Lever/fixture, and Oracle/example, plus `live confirmation` and a manifest-bound sanitized confirmation artifact path. RED: the five-platform focused test failed with `ModuleNotFoundError`, and the CLI test then failed because `confirmation` was not a live subcommand. GREEN: the six reader/fallback tests and CLI test passed; full suite `python -m pytest tests -q` → `299 passed in 1.17s`. Readers validate learned-handler confirmation, then require one exact Candidate Home record with both explicit submitted indicators; raw HTML/portal payloads are discarded. Unverified tenants, missing/failed readers, ambiguous identity/state, and uncertain confirmation produce stable `human_required` evidence with `safe_for_post_submit: false`. Only sanitized fixtures and injected pages were read. Next exact task: add explicit-commit real tracker and Discord adapters with authenticated idempotent read-back while keeping tests local.
- 2026-08-27 03:01 PDT — Completed unified CLI task 7: added `live_delivery_adapters.py` with commit-gated Google Sheets and Discord transaction adapters plus `live deliver`. RED: the adapter test failed with `ModuleNotFoundError`, then the CLI test failed because `main` lacked injectable delivery adapters. GREEN: two adapter tests and the local CLI transaction test passed; full suite `python -m pytest tests -q` → `302 passed in 1.23s`. Both external adapters require the exact `commit_external` capability, perform authenticated read-back, embed transaction/hash idempotency markers, and refuse a changed or duplicate payload; Discord obtains its token only from a runtime provider. The CLI enforces portal confirmation and tracker read-back before Discord, forbids external commit for sanitized manifests, and requires production enablement plus explicit external commit for production manifests. Tests used only local fake backends/adapters; no credentials, tracker, network, or message service was accessed. Next exact task: add `live resume/status` with read-back-only recovery across every stage.
- 2026-08-27 03:05 PDT — Completed unified CLI task 8: added value-free `live status`, bounded `live resume`, and read-only transaction-state inspection. RED: the focused test failed because `status` was not a live subcommand. GREEN: the focused status/recovery test passed; full suite `python -m pytest tests -q` → `303 passed in 1.13s`. Status derives preparation, Review, authorization handoff, submit journal, confirmation, tracker, and Discord states from exact manifest paths and durable flags. Resume can invoke only confirmation inspection after any submit intent or coordinator read-back for claimed downstream attempts; it never invokes prepare, Review, authorize, or submit. The test used an inspection-only page whose submit method raises, proving uncertain-submit recovery cannot click. No external system or credential was accessed. Next exact task: add a complete local real-Chrome CLI sequence/failure matrix and a normal-Chrome read-only exact-target preflight.
- 2026-08-27 03:09 PDT — Completed unified CLI task 9: added `tests/test_production_operator_live_chrome.py` and read-only `live preflight`. The composed real-Chrome CLI sequence passed immediately over Tasks 1–8; the new preflight behavior supplied the required RED by failing with `TypeError: main() got an unexpected keyword argument 'live_readonly_transport_factory'`. GREEN: both Task 9 tests passed in `0.71s`; full suite `python -m pytest tests -q` → `305 passed in 0.88s`. Marker-checked ephemeral Chrome proved overlay blocking, exact field/upload read-back, authoritative Review, protected authorization, submit intent before one click, replay denial, learned confirmation/portal proof, local tracker/Discord order, idempotent delivery, terminal status, and submit count one. Normal-Chrome preflight uses only two fresh read-only exact-target snapshots and proves unchanged content/identity without page mutation. The Playwright skill's fresh-snapshot guidance informed the verification, while the repository's stricter process-scoped CDP harness remained the only browser control. No external page, credential, tracker, or message was accessed. Next exact task: finalize persistent runbooks/job-application skill and release audit with real-live disabled unless all gates are explicit.
- 2026-08-27 03:20 PDT — Completed unified CLI task 10 and the queue: added closed `live release-audit` evidence validation, consolidated the exact numbered live command/approval/health/recovery procedure in README/OPERATIONS, and updated the persistent Hermes `job-application-automation` skill to v2.1.0 with a routed unified-CLI reference. RED: the focused test first failed because `release-audit` was not a valid live subcommand; a follow-up malformed-schema case exposed an unhandled nested list. GREEN: the focused audit test passed with malformed evidence returning `not_ready`; full suite `python -m pytest tests -q` → `306 passed in 1.28s`. The marker-checked local real-Chrome sequence passed in `0.67s` with one submit/replay denial, and the injected read-only normal-Chrome preflight passed in `0.03s` with unchanged exact-target evidence. The broad executable-source scan found only the existing safety docstring denying raw desktop input; the precise prohibited import/call scan returned no matches, `git diff --check` passed, and direct Hermes frontmatter/reference integrity validation passed. A valid release result remains only `ready_for_manual_live_authorization_review` and always returns `real_live_enabled: false`, `commit_external_enabled: false`, and `real_application_authorized: false`. No real ATS, credential/account, tracker, Discord/email, or production data was accessed or changed. Next exact task: run the post-commit release artifact, ordered-history, full-suite, whitespace/source, and clean-worktree verification; no queue implementation item remains.
