# Ashby Date Picker

For an Ashby `react-datepicker` graduation date, a typed date can display the intended month/year while the calendar remains open. Select the actual rendered day row (`.react-datepicker__day` / `role="option"`), then use server validation to confirm the graduation-date field is no longer reported as missing. A displayed value alone is not proof of a bound answer.
